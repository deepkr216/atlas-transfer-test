# Lessons - wrong facts we have produced, and the test that now prevents each

The rule: when a wrong fact is found (by a human, by the citation gate, by a
smoke run), the fixture and the failing assertion land in `tests/` **first**,
then the fix, then a row here. Nothing is "fixed" until the test exists.

| # | Wrong fact produced | Root cause | Guarded by |
|---|---|---|---|
| 1 | Identifiers made of change-ticket stamps; statements never terminating | Identification area (cols 73-80) leaked into code because records were 79 columns, not 80 | `RaggedMargins.test_identification_area_does_not_leak` |
| 2 | Every copybook field at offset 0 | A copybook *fragment* (05s with no 01) parsed as separate roots | `PMASTREC` offsets in `test_multiline` |
| 3 | Variable-length table indexed as fixed | `OCCURS ... DEPENDING ON` on a continuation line without a hyphen was dropped | `test_occurs_depending_on_across_lines` |
| 4 | Paragraph `1000-EXIT` missing from the PERFORM graph | `1000-EXIT.  EXIT.` treated as one statement | `test_paragraph_header_and_statement_on_same_line` |
| 5 | Single-word statements (`EXIT.`) indexed as paragraphs | Area A check without a reserved-word guard | same test |
| 6 | `PIC ZZZ,ZZ9.99` truncated to `ZZZ,ZZ9` | Period inside a picture string / numeric literal treated as terminator | `test_pic_with_embedded_period_is_not_split` |
| 7 | `PROGRAM-ID` unknown when the name is on the next line | Name looked for in the same statement only | `test_program_id_on_following_line` |
| 8 | Commented-out `CALL 'OLDRATER'` reported as a live dependency | Column-7 `*` ignored | `CommentedCode.test_commented_call_is_not_an_edge` |
| 9 | Dynamic `CALL WS-SUB-PROGRAM` reported as calling nothing | Only one literal traced; VALUE clause missed | `test_dynamic_call_resolves_every_candidate` |
| 10 | Continued literal lost its first character | Fixture literal extended into column 73 - the *compiler* would drop it too; fixture was wrong, parser right | `test_column7_hyphen_literal_continuation` |
| 11 | `MOVE WS-CALL-FLAG TO WS-KEY` indexed as a dynamic CALL to program `TO` | `\b` treats `-` as a word boundary; every verb pattern needed hyphen-aware lookarounds | `test_identifier_containing_call_is_not_a_call` |
| 12 | DCLGEN host structures invisible | `EXEC SQL INCLUDE` not treated as a copy; `++INCLUDE`/`-INC` live in the sequence area | `test_exec_sql_include_is_a_copy` |
| 13 | DD name `UT-S-CLMFILE` never joined to the JCL | ASSIGN device/organisation prefixes not stripped | `test_assign_with_device_prefix_yields_ddname` |
| 14 | Host variables harvested as DB2 tables | `INTO` matched in `SELECT ... INTO :hv` | `test_select_into_hostvar_is_not_a_table` |
| 15 | Column `PREM_AMT` reported as a table | `FOR UPDATE OF col` matched as `UPDATE table` | `test_for_update_of_is_not_a_table` |
| 16 | Second `MOVE` inside `IF ... ELSE ... END-IF` not indexed | Only the first verb of a compound statement dispatched | `test_both_moves_inside_compound_if_are_captured` |
| 17 | Both MOVEs cited to the IF's line | Statement start used as the line for every verb inside it | same test (asserts lines 30 and 32) |
| 18 | Batch lineage arrows reversed | `DISP=OLD/SHR` read as input, `NEW` as output; DISP is not direction | `JclDirection.test_disp_old_is_not_direction`, `test_gdg_plus_one_is_output` |
| 19 | Sort steps treated as pass-through | Control-card byte positions not extracted | `test_sort_card_byte_positions` |
| 20 | DBD/PSB members classified as `unknown` | Signatures anchored on leading whitespace; HLASM puts the label in column 1 | `test_classify_hlasm_label_and_level_49` |
| 21 | DCLGEN copybooks with level 49 misfiled | Classifier tested only levels 01/05/10/15 | same test |
| 22 | PSB `KEYLEN` missing | Column-72 continuation not joined | `test_psb_column72_continuation_and_cmpat` |
| 23 | Wrong database named for a DL/I call | I/O PCB shifts positions under `CMPAT=YES`/TP; region type decides otherwise | same test + `test_psb_without_cmpat_is_undetermined` |
| 24 | Fields renamed by `COPY ... REPLACING` untraceable | No expanded source | `CopybookExpansion.test_replacing_and_line_map` |
| 25 | Citations drift after expansion | Line numbers not mapped back to the including member | same test (asserts origin of a post-COPY line) |
| 26 | Missing copybook silently ignored | Expansion swallowed the failure | `test_missing_copybook_is_a_warning_not_silence` |
| 27 | Legacy `.doc` counted as "indexed, empty" | No extractor, no report | `test_legacy_binary_is_reported_not_silent` |
| 28 | A model's citation to a comment line accepted | Gate checked text presence only | `CitationGate.test_citation_to_commented_line_warns` |
| 29 | `WHEN 3 ALSO 'M'` attributed `'M'` to the relationship field | EVALUATE subject taken as one field; ALSO positions ignored | `ConditionLogic.test_evaluate_also_attaches_when_values_by_position` |
| 30 | `IF A = 1 AND B = 'M' OR 'F'` recorded `'F'` against A as well as B | Abbreviated-condition scan ran to end of statement instead of to the next compare | `test_abbreviated_or_after_and_is_not_over_attributed` |
| 31 | `messages GENDER` missed `RELATIONSHIP/GENDER MISMATCH` built from two FILLER VALUEs | Each piece indexed, the whole never assembled | `test_message_assembled_from_fillers`, `ScreensEndToEnd.test_messages_finds_filler_assembled_text` |
| 32 | Screen validation invisible: program tests `GENDERI`, map field is `GENDER` | BMS symbolic names not derived | `BmsMfsParsing.test_bms_map_fields_defaults_and_symbolic_names`, `test_screen_dossier_links_bms_field_to_validating_program` |
| 33 | MFS field offsets wrong by 2 on output messages | `ATTR=YES` attribute bytes not counted | `test_mfs_offsets_and_type_inferred_from_fip_fop_label` |
| 34 | MFS message with no `TYPE=` classified as unknown direction | Shop names MIDs/MODs `…FIP`/`…FOP`; inference from the label with a recorded warning | same test |
| 35 | Screen labels (`'GENDER:'`) and defaults (`INITIAL='U'`) absent from message/value searches | Screen literals not indexed as literals | `test_values_on_a_screen_field_shows_default_and_validator` |

| 36 | Online programs listed as dead; "which transaction runs this" unanswerable | No routing source loaded | `RoutingParsing.*`, `RoutingEndToEnd.test_online_programs_are_no_longer_dead_candidates` |
| 37 | Re-running `build` without `--rebuild` duplicated every fact and every FTS row | `INSERT OR REPLACE` on member plus FTS rows with no member key | `IncrementalBuild.test_rerun_is_idempotent_and_changes_propagate` |
| 38 | A changed copybook left programs with facts derived from its OLD text | Incremental skip did not follow COPY dependencies | same test (asserts `exp_lines` grows after the copybook changes) |
| 39 | A dataset the CSD declares as VSAM stayed `is_vsam=0` | `INSERT OR IGNORE` after the JCL had created the row | `RoutingEndToEnd.test_cics_file_maps_to_dataset` |
| 40 | **Every `//PROCSTEP.DDNAME` override silently dropped** | JCL statement regex allowed an 8-character name only; qualified names never matched | `ProcExpansion.test_included_dd_overrides_proc_dd`, `test_qualified_override_and_added_dd`, `test_inline_sysin_override_replaces_dummy` |
| 41 | DSNs in cataloged PROCs stayed `&HLQ..MASTER`; jobs showed no datasets | Members resolved in isolation; no PROC expansion with the calling job's symbolics | `ProcExpansion.test_exec_override_beats_proc_default`, `JobLevelLineage.*` |
| 42 | `&&TEMP` treated as a symbolic and reported unresolved | Symbol regex matched the second `&` | `test_temporary_dataset_is_not_a_symbolic` |
| 43 | VSAM files had no origin: lineage began at the first reader | IDCAMS `DEFINE`/`DELETE`/`REPRO` cards not read | `test_idcams_ops`, `test_job_dossier_shows_effective_steps` |
| 44 | "Where is column X populated" unanswerable: only table names indexed | No column ↔ host-variable pairing | `ColumnLineage.*`, `ColumnQueries.test_column_report` |
| 45 | A message built by `STRING` across four lines invisible to `messages` | Only the literal pieces were indexed, never the assembled text | `test_string_and_display_templates`, `test_messages_finds_runtime_templates` |
| 46 | A program with no source (load module only) reported NOT FOUND although JCL runs it | Dossier required a program row | `test_program_runs_in_shows_the_job_not_the_bare_proc` |
| 47 | A DB2 batch step "runs IKJEFT01", no sort fields, no IDCAMS ops, because the cards are in `PROD.PARMLIB(MEMBER)` not `DD *` | Only inline data became `sysin_text`; the `(MEMBER)` reference was never followed to the indexed card member | `test_card_member_text_is_attached_to_the_dd`, `test_launcher_resolves_through_the_card_member`, `test_card_member_is_findable_by_name` |
| 48 | "PROC SRTPROC not found" and the PROC's steps counted as the job's own steps, for a `// PROC … // PEND` coded inside the job | Instream PROCs were flattened into the job instead of collected and expanded by `EXEC` | `test_instream_proc_is_collected_not_flattened_into_the_job`, `test_instream_proc_shadows_cataloged_proc` |
| 49 | `dataset SORTED` joined two unrelated jobs through `&&SORTED`; `&&TEMP` looked like an estate dataset | Temporary datasets are job-scoped but the `dataset` table is global | `test_temp_is_job_local`, `test_temp_never_becomes_a_dataset_row` |
| 50 | A step reading `DSN=*.STEP1.SORTOUT` showed no dataset - a hole in lineage exactly at the job's own intermediate file; a referback to a `(+1)` inherited the `+1` and looked like a second writer | Referbacks were kept as text; direction was copied from the source DD | `test_referback_inside_the_proc_follows_the_temp`, `test_referback_across_the_proc_boundary_reads_the_created_generation`, `test_bad_referback_is_reported_not_dropped` |
| 51 | `DSN=PROD.X   INPUT MASTER` indexed as dataset `PROD.X   INPUT MASTER`; `PGM=PGMB,   RUN STEP TWO` + `//  PARM='Y'` lost the PARM and produced a bogus statement | The operand field was read to column 71 instead of ending at the first blank outside quotes/parentheses | `Reader.test_operand_field_ends_at_the_first_blank` |
| 52 | A PARM continued inside its quotes at column 71 (no comma) was silently truncated | Continuation was recognised only by a trailing comma | `test_quoted_parm_continued_at_column_71` |
| 53 | `job JOBA` NOT FOUND and JOBB credited with JOBA's datasets, for a member holding two JOB cards | The second JOB card overwrote the first job's name | `test_several_job_cards_in_one_member`, `test_job_stream_member_yields_two_jobs` |
| 54 | "Which load library ran" unanswerable for jobs using JOBLIB instead of STEPLIB | DDs before the first EXEC were dropped | `test_joblib_reaches_every_step_without_steplib`, `test_joblib_reaches_expanded_steps_without_steplib` |
| 55 | Error / backout / notify steps inside `// IF (S1.RC > 4) THEN` narrated as the normal flow of the job | IF/ELSE/ENDIF and JOB-card COND were parsed and thrown away | `test_if_then_else_guards_and_job_cond`, `test_job_dossier_shows_joblib_guard_and_cond` |
| 56 | `EXEC NIGHTLY,PARM.PS010='JOBPARM'` reported the PROC's default PARM; a bare `PARM=` on EXEC PROC never reached the first step | Qualified PARM./COND. were stored as symbolics named `PARM.PS010` | `test_parm_and_cond_qualified_overrides`, `test_bare_parm_on_exec_proc_goes_to_the_first_step_only` |
| 57 | A STEPLIB override concatenation `TEST.LOAD` + `PROD.LOAD1` came out as `PROD.LOAD1, PROD.LOAD1` - the test library vanished; `//PS.SYSIN DD DUMMY` still showed the PROC's card dataset as read | Overrides were keyed by DD name only; DUMMY had no DSN so the PROC's DSN was kept | `test_concatenation_override_entry_by_entry_and_dummy` |
| 58 | `EXEC NIGHTLY,HLQ=OVR` changed `&HLQ` in the job's own later steps, overriding the SET value the system uses | Symbolic overrides were written into the member-wide symbol table | `test_override_does_not_leak_into_later_job_steps`, `test_override_does_not_leak_into_the_job_after_expansion` |
| 59 | `PARM='&DT'` and `PGM=DFSRRC00,PARM='DLI,&PGM,&PSB'` kept `&` text although `SET` defined them; run-time symbols in DSNs (`%%ODATE`, `&LYYMMDD`) became literal dataset names with no note | Job-level PARM/PGM were never substituted; residual symbols were not reported or normalised | `test_job_level_parm_pgm_symbols_and_runtime_symbols` |
| 60 | Every dataset appeared "written" by its IEFBR14 cleanup step, and cross-department flow fired on a delete | DISP=(MOD,DELETE) was read as direction `mod` | `test_iefbr14_is_not_a_writer`, `test_iefbr14_delete_is_not_a_writer_in_dataset` |
| 61 | `DSN=PROD.CLM.DUMMY.FILE` treated as DD DUMMY (link dropped); `DSN=NULLFILE` indexed as a real dataset | `DUMMY` was a substring test | `test_dummy_is_the_first_operand_not_a_substring` |
| 62 | TSO `CALL 'PROD.LOAD(CLMFIX)'` steps had no program; the second `RUN PROGRAM` in one SYSTSIN was lost; `RUN PROGRAM(DSNTIAUL)` looked like an application program | Only the first `RUN PROGRAM(` was matched | `test_tso_call_and_two_runs`, `test_db2_utility_run_under_tso_is_not_a_program` |
| 63 | Image-copy / unload / reload steps (`PARM='ULU,DFSUDMP0,CLMDBD'`) reported "PSB CLMDBD" | Utility regions name a DBD in the third position | `test_ims_utility_region_names_a_dbd_not_a_psb` |
| 64 | Easytrieve steps: `program EZTPA00` ran in 300 jobs; the report program's files and byte positions were invisible | EZTPA00 was an ordinary program | `test_easytrieve_step` |
| 65 | Batch FTP steps reported "no inline cards"; where cards existed the userid and password were stored and printed into packs; Connect:Direct process members were never followed | Only SYSIN/STDIN were read; nothing redacted; `SUBMIT PROC=` not followed | `test_ftp_reads_input_dd_and_never_keeps_the_password`, `test_connect_direct_process_member_is_followed` |
| 66 | `EXEC INNER,HLQ=&HLQ` inside OUTER produced dataset `&HLQI` and blamed a symbolic; OUTER's defaults overrode INNER's own | Override values were not resolved through the enclosing symbols; outer defaults were passed down as if SET | `test_nested_proc_passes_the_value_not_the_symbol` |
| 67 | Rerun JCL reading `PROD.G.G0012V00` was not a reader of the GDG | Only relative generations were normalised | `test_absolute_gdg_generation_joins_the_gdg` |
| 68 | POLICY's `POLNIGHT` expanded with CLAIMS' `NIGHTLY` - CLMPOST and PROD.CLM.MASTER credited to a POLICY job, nothing reported | PROCs and INCLUDEs resolved by bare name, first hit; `JCLLIB ORDER` not parsed | `test_jcllib_order_picks_the_departments_proc`, `test_same_department_wins_without_jcllib`, `test_ambiguous_proc_is_reported_when_nothing_decides` |
| 69 | `dataset CLM.MASTER` listed `TEST.CLM.MASTER` (a PROC default nobody runs) next to the real one, and a job step's own `//PS.DD` override twice | Bare-PROC rows and EXEC PROC step overrides were not distinguished from job-level rows | `test_dataset_lineage_credits_the_right_job` |
| 70 | `EXEC CICS READ FILE('X') INTO(Y)` produced a COBOL file op on a file called `FILE`, `START TRANSID` a file called `TRANSID`, `EXEC SQL CLOSE POLCUR` a file CLOSE; `EXEC SQL CALL PRD.POLPROC` became an unresolved dynamic CALL to `PRD` | EXEC spans were fed to the COBOL verb extractors | `ExecMasking.test_exec_text_is_not_cobol` |
| 71 | `OPEN INPUT A B OUTPUT C` recorded only A; `WRITE OUT-REC` never reached the file OUT-FILE (so no writer, no OPEN-verb direction); SORT INPUT/OUTPUT PROCEDURE paragraphs "never performed" | One identifier per verb; `fd_record` was the FD's own name | `FileIO.test_open_close_lists_and_record_owner` |
| 72 | The paragraph after `EJECT` vanished; `AUTHOR. PAT O'BRIEN.` opened a literal that swallowed the whole program (parse status ok, zero facts) | Compiler-directing lines and ID-division comment-entries were treated as code | `ReaderDirectives.test_eject_and_author_apostrophe` |
| 73 | `CALL 'A' USING X ELSE CALL WS-B USING Y END-IF` gave A the arguments `X ELSE CALL WS-B USING Y END-IF` and dropped the second CALL; `LENGTH OF` / `ADDRESS OF` / subscripts became arguments; `WS-KEY OF WS-REC = 'B'` was a test of WS-REC; a target set by `SET WS-PGM-SUBA TO TRUE` was "unresolved" | One CALL per sentence; USING cut only at END-CALL/period; 88 values not mapped to the parent | `CallParsing.test_using_list_stops_and_every_call_is_seen` |
| 74 | `PERFORM 1000-PROCESS` (a SECTION) pointed at nothing and its paragraphs were "never performed"; paragraphs reached only by `GO TO` or by falling through were dead candidates; `PERFORM 3 TIMES` created an edge to `3` | Sections not recorded; GO TO / fall-through not modelled | `ControlFlow.test_sections_goto_times_fallthrough` |
| 75 | `01 WS-AMOUNTS COMP-3.` children sized as DISPLAY (9 bytes not 5); `WS-BINARY-CNT` made its item COMP; `05 PIC X(3).` became a zero-length field named PIC; a `77` after an 01 became its child; `88 X VALUES 'A' 'B'` had no values; `PIC ZZ9.99DB` one byte short | Group USAGE not inherited; usage matched inside names; anonymous entries; 77 not a root; VALUES/ARE; CR/DB one char | `CopybookArithmetic.test_group_usage_anonymous_filler_77_values_crdb` |
| 76 | `COPY PMASTREC REPLACING ==PM-== BY ==LK-==` never renamed anything (pseudo-text was matched as a whole word); `==01== BY ==05==` rewrote `PIC X(01)`; a COPY word inside a DISPLAY literal was expanded; and after a real rename `field PM-POLICY-STATUS` showed no references from the program | Pseudo-text vs word not distinguished; no alias link | `Replacing.*`, `ReplacingAliasesEndToEnd.*` |
| 77 | `CALL 'CBLTDLI' USING DLI-GU ...` (function in a VALUE field) was "function held in a variable, read vs update unknown"; with a parm-count first argument the PCB named was the function field | Literal map not consulted; positional shift not handled | `test_dli_function_from_value_and_parmcount` |
| 78 | A CHNG + ISRT message switch left the target transaction "dead"; `EXEC DLI` kept only the verb (no PCB, segment or I/O area) | CHNG not in the function set; EXEC DLI operands dropped | `test_message_switch_and_exec_dli`, `test_message_switch_reaches_the_transaction` |
| 79 | Every DL/I call showed "map the PCB through the PSB yourself": the off-by-one (I/O PCB first, LIST=NO skipped) was left to the reader | PCB position never resolved; `ENTRY 'DLITCBL' USING` not parsed | `test_pcb_position_resolved_with_io_pcb_first`, `test_entry_dlitcbl_gives_the_pcb_list`, `test_psb_list_no_and_procseq` |
| 80 | A GSAM extract had no writer in `dataset`; DBD FIELDs / XDFLDs / DATASET DD were parsed and thrown away; a member with two DBDs kept the last name with the first segments | Nothing joined a GSAM PCB to its DD; DBD detail not stored | `test_gsam_pcb_sets_the_dd_direction`, `test_dbd_dataset_fields_xdfld_and_multi_dbd` |
| 81 | `screen MEMMAP` could not name the program that SENDs it; `transaction MEM2` did not know it is reached by `RETURN TRANSID`; `EXEC CICS READ ... INTO(X)` gave X no writer; LINK's COMMAREA never entered the value flow | EXEC CICS facts were parsed and never persisted | `test_cics_commands_and_host_fields`, `test_cics_chaining_maps_and_dead` |
| 82 | `dataset PROD.POLICY.MASTER.KSDS` listed batch jobs only - the CICS programs that REWRITE it all day were absent; a TD queue's dataset had no writer | `dataset` read the JCL view only; TDQUEUE/DB2ENTRY/URIMAP blocks were discarded | `test_dataset_shows_online_readers_and_tdq`, `test_csd_extra_resources_and_blank_before_paren` |
| 83 | `SELECT * INTO :DCLPOLICY` gave no column lineage; `PRD.POLICY_TBL` and `POLICY_TBL` were two tables; `FETCH ... FROM POLCUR` made the cursor a table | DCLGEN DECLARE TABLE dropped; group host variables not expanded; qualifier kept in the key | `test_sql_dclgen_group_into_fetch_rowset_and_call`, `test_dclgen_select_star_and_table_query` |
| 84 | MQ edges said `MQPUT` and nothing else - no queue, no message layout | USING arguments of MQ calls not read; MQOD-OBJECTNAME not resolved | `test_mq_queue_and_layout`, `test_mq_interface_edge_names_the_queue` |
| 85 | An MFS MID whose MFLDs omit `LTH=` put every field at the same offset; `ATTR=(YES,n)` added nothing | DFLD lengths not inherited; only the string form `YES` handled | `test_mfs_inherited_lengths_and_extended_attributes` |
| 86 | The gate rejected `[[SAMPPGM:27 "..."]]` - the exact form every report prints - as "no citation"; `[[SAMPPGM 27 ...]]` FAILED as "AMBIGUOUS (3 copies)" because a PSB and a job share the program's name | Citation regex required a blank; members resolved by name without kind | `GateAndCite.test_forms_and_kind_resolution`, `test_cite_prefers_the_program_and_names_the_others` |
| 87 | With CLAIMS and POLICY both authoritative, `[[DUPREC 1 "PIC X(9)"]]` silently PASSED against whichever copy sorted first | Authoritative rows tie-broke by order | `test_ambiguous_departments_and_qualifiers` |
| 88 | `[[SAMPJOB 1-34 "DD"]]` PASSED (2-char token, whole member); a `//*` comment line PASSED; a re-fetched member with shifted lines PASSED every old citation | No weak-token / range / JCL-comment checks; on-disk sha not compared with the index | `test_warnings_weak_wide_comment`, `test_changed_file_fails` |
| 89 | `dataset` / `copybook` / `callers` / `program` DB2 rows carried no line, so the core impact statements were uncitable | Reports printed facts without their source line | `test_dataset_and_copybook_rows_carry_cites`, `test_callers_cites_and_argument_check` |
| 90 | `field POL-NO` showed 12 references and "..." - the programs dropped were unknowable; `field PM-LAPSED` (an 88 name) said NOT DEFINED | Fixed truncation; 88 names not looked up | `test_field_groups_programs_and_answers_an_88_name` |
| 91 | `job NIGHTLY` never said which jobs execute the PROC; `literal E101` never showed the message text beside the code | PROC callers and FILLER siblings not joined | `test_job_proc_says_who_executes_it`, `test_literal_shows_the_message_text_beside_a_code` |
| 92 | A pack could exceed the month's token budget, carried full laptop paths, existed for programs only, and omitted every IF/WHEN a tester needs | No budget, no path stripping, no job/copybook packs, no COND picks | `test_packs_budget_header_paths_kinds` |
| 93 | "Who calls RATECALC and does every caller pass 4 parameters" needed the model to read every caller | No call-site view against the callee's LINKAGE | `test_callers_cites_and_argument_check` |
| 94 | The D1 CRUD matrix, the T1 test-condition inventory and "explain this paragraph" were assembled by the model from raw text | No `crud` / `conditions` / `paragraph` queries | `test_crud_conditions_paragraph` |
| 95 | `coverage` said `ctlcard skipped 3100` without saying that class contributes no facts; `dead` covered programs only | No handler-vs-kind table, no optional-input checklist, no dataset/copybook/job decommissioning lists | `test_dead_and_coverage_extras` |
| 96 | A section's paragraphs and GO TO / fall-through targets were counted "never PERFORMed" | The structure line used PERFORM edges only | `ControlFlow.test_sections_goto_times_fallthrough` |
| 97 | A job's `// SET HLQ=PROD` was applied inside a PROC whose statement says `HLQ=TEST` - the production/test confusion the tool exists to prevent, in the tool's own answer | Precedence coded as SET > PROC default; z/OS applies EXEC override > PROC default > SET (`jcl.PROC_DEFAULT_BEATS_SET` explains how to confirm with one job) | `test_proc_default_beats_job_set_in_parm` |
| 98 | `EXEC DLIBATCH,MBR=CLMPOST,PSB=CLMPSB` (the normal IMS batch form) gave "PROC not found", no effective program, CLMPOST a dead candidate | IMS.PROCLIB is not in the estate folder and nothing read the MBR=/PSB= overrides | `test_system_proc_synthesised` |
| 99 | `SYSOUT=(A,INTRDR)` was a print class; the submitted job had no predecessor | INTRDR not recognised as a submit | `test_intrdr_submit_edge` |
| 100 | `DD PATH='/u/feeds/in.txt'` had no dataset and no direction - the USS landing zone of every FTP/BPXBATCH feed was invisible | PATH= not read | `test_uss_path_dd` |
| 101 | ICETOOL steps were "SORT with no inline SYSIN"; OUTFIL FNAMES= outputs, JOINKEYS inputs and ICETOOL FROM/TO had DISP-only direction; `OUTREC=(1:1,10,11:21,5)` yielded no byte positions; SYMNAMES symbols yielded nothing | Only SYSIN read; column-positioned items rejected by a lookbehind; symbols not substituted | `test_sort_cards_roles_symbols_positions` |
| 102 | `LOAD ... INTO TABLE X` and DSNTIAUL's `SELECT ... FROM X` were invisible: `table X` listed COBOL programs only, the unload file had DISP-only direction | Utility cards not parsed | `test_db2_utility_lineage` |
| 103 | IDCAMS `RECORDSIZE`, `KEYS`, `LIMIT`, `RELATE` were dropped, so a copybook length could not be checked against the cluster | Only the DEFINE verb and name were kept | `test_idcams_attributes` |
| 104 | Every member of `PROD.CLAIMS.CNTL` became a job; a BIND deck in `PROD.CLAIMS.BIND/x.txt` became a specification; a compiler listing became a second copy of the program | Folder hint `CNTL` -> jcl; `.txt` -> doc regardless of folder; no listing signature | `Classification.test_cntl_listing_unknown_and_jcl_by_content` |
| 105 | A 0-byte or comment-only `.cbl` got a program row and outranked the real program; a card deck filed in the source library got a program row too | Folder hint decided the kind; `index_cobol` inserted a program for any member | `BuildLevel.test_kinds_and_no_phantom_rows` |
| 106 | `out/expanded/CLMPOST.exp.cbl` was re-indexed on the next build as a second CLMPOST | The toolkit's own outputs were not marked | `test_kinds_and_no_phantom_rows` |
| 107 | After `git pull` the index kept facts made by the previous parser; after a PROC change every job that expands it kept the old steps | Incremental build compared member bytes only | `test_proc_change_forces_jobs_and_fingerprint_forces_all` |
| 108 | A library whose download stopped at member 2,900 of 4,100 was indexed as complete; a program deleted on the host years ago stayed live; a listed-but-missing program was "NOT FOUND" | Nothing compared the folder with the host's member list | `FetchReconcile.test_reconcile_writes_marker_and_moves_stale`, `test_library_marker_incomplete_and_not_fetched` |
| 109 | A hand-written `manifest.json` was silently overwritten by every UI build | The generated manifest used the same path | `test_hand_written_manifest_is_kept` |
| 110 | `--password x` in extra_args was echoed into the live log and the plan | Command lines were logged verbatim | `Classification.test_redact_cmd` |
| 111 | Every report read as current, even from a build that crashed halfway or a download three months old | No "as of" line anywhere | `test_library_marker_incomplete_and_not_fetched` (index header) |
| 112 | `-- get the policy.` inside EXEC SQL ended the COBOL statement: the SELECT after it was never seen | Line ends vanish when a statement is assembled; the period in the SQL comment looked like a terminator | `test_sql_comment_with_period_does_not_end_the_statement` |
| 113 | Every DCLGEN copybook was flagged "contains procedure code" because its `DECLARE TABLE` is EXEC SQL; its column list was thrown away | DECLARE counted as SQL to run | `test_dclgen_is_not_procedure_code_and_declares_columns` |
| 114 | `job CLMINC` (an INCLUDE member) said NOT FOUND; nothing said which jobs splice it in | INCLUDE use not stored | `test_include_member_says_who_includes_it` |
| 115 | `//SYSIN DD DSN=PROD.CLAIMS.SRTCLM` (cards in a SEQUENTIAL dataset) had no cards even when the file was fetched | Only `LIB(MEMBER)` was looked up | `test_sequential_card_dataset_matched_by_last_qualifier` (recorded as an assumption) |
| 116 | "What leaves the mainframe" had no answer: FTP/NDM/MQ/TDQ/web facts were scattered across dossiers and the peer system was nowhere | No `interfaces` report; no place to declare peers | `test_interfaces_report_and_dataset_boundary_line` (manifest `external_interfaces`) |
| 117 | A crash report still carried `PROD.MASTER` (two qualifiers) and job / copybook / program names | Redaction knew three-qualifier DSNs and member names only | `test_diag_redacts_two_qualifier_dsns` |

## Known gaps (not yet guarded - contributions welcome)

- SYNC alignment slack is flagged, not computed.
- GO TO and fall-through are not evaluated for dead-paragraph candidates.
- IMS program name = PSB name is an assumption (recorded per row); `APPLCTN GPSB=` is handled, `PGMTYPE=BATCH` APPLCTNs without transactions are listed as programs only.
- MFS `DO` repeats are expanded with the default 2-digit suffix only; `SUF=` and `BOUND=` are not modelled.
- Zowe command syntax is built from the documented `zos-files download all-members / data-set` forms; a shop's profile type or flags go in `extra_args` — run `--check` and `--plan` before the first real fetch.
- PDF text with CID fonts may be garbled; the extractor says so but cannot fix it.
- Compiler listings are the authoritative expansion; a listing loader would supersede `expand.py`.
| 118 | A pack redirected with PowerShell 5.1 `>` arrived as UTF-16 (`python -m atlas.query ... > pack.md`); the chat model saw NUL-separated characters and the gate read garbage | The shell, not the toolkit, chose the file encoding | `test_vscode.py::test_query_out_writes_utf8` - `--out FILE` on `atlas.query` and `atlas.verify_citations` writes UTF-8 with LF and creates the folder, independent of the shell |
| 119 | `walk` printed the last paragraph as `WALKPGM:62 .. None:72` with a trailing blank line: the parser sets the final paragraph's end_line to the logical line count, which runs past the expanded text | A paragraph range was trusted beyond the line map | `test_walk.py::test_source_lines_keep_indentation_and_stop_at_the_last_real_line` - the block ends at the last mappable non-blank line; the span is the cite range |
| 120 | `walk` listed `CLAIM-FILE` under "referenced but not defined" fields | OPEN / READ / CLOSE operands land in `field_ref` like any identifier | `test_walk.py::test_data_holds_only_touched_fields` - SELECT names are filtered out of the data section |
| 121 | `walk --budget 4596` returned 5124 characters | The fit loop counted the blocks but not the leading comment (token count, index header) nor `## Walk`; when every source was already gone it stopped instead of cutting the data tables | `test_walk.py::test_budget_keeps_order_and_facts_drops_source_from_the_end` - length <= budget + 60; the header says what was cut, or by how much the skeleton alone exceeds the budget |
| 122 | `PERFORM WORK OF SUB-B` was walked as SUB-A's WORK (the first paragraph of that name); SUB-B's WORK was never shown and its section was listed as never entered | The parser dropped the OF/IN qualifier from PERFORM targets; `GO TO WORK OF SUB-B` even produced edges to `OF` and `SUB-B` | `test_walk_shapes.py::test_qualified_perform_and_goto_reach_the_right_paragraph` - targets keep the qualifier as `WORK OF SUB-B`; the walk resolves by (name, section), preferring the caller's own section for a bare name |
| 123 | `GO TO B100 B200 DEPENDING ON WS-K` ended its paragraph: the next paragraph was "reached by nothing" | `_TERMINAL` matched every GO TO; a DEPENDING GO TO falls through when the index is out of range | `test_walk_shapes.py::test_goto_depending_falls_through_when_out_of_range` |
| 124 | A section whose paragraph was PERFORMed was listed as "never PERFORMed and never entered" | The section header row was never visited, only its paragraph | `test_walk_shapes.py::test_qualified_perform_and_goto_reach_the_right_paragraph` - a section with a visited paragraph is not "not reached" |
| 125 | The last paragraph of the ENTRY section did not fall through into the next section | The walk applied the "a PERFORMed section returns after its last paragraph" rule to every section, including the one the program starts in | `test_walk_shapes.py::test_entry_section_keeps_flowing_a_performed_section_returns` |
| 126 | A procedure copybook paragraph renamed by `COPY ... REPLACING ==XX-== BY ==LK-==` printed as LK-PARA with source reading `XX-PARA.`: a model citing `[[RPROC2 1 "LK-PARA"]]` fails the gate; and a copybook's trailing empty record was printed as a blank source line | The header carries the program's name, the lines carry the copybook's text; blank lines are not in `src_fts` | `test_walk_shapes.py::test_replacing_renamed_copybook_paragraph_says_so_and_blank_lines_vanish` - the block says what the copybook line reads; blank lines are not printed |
| 127 | Fetch all created the local folders and downloaded no members, while `zowe zos-files download all-members "PDS" -d folder` typed by hand worked | First suspect: the toolkit added `--extension` and `--max-concurrent-requests 8` to every download, and a Zowe CLI that does not know a flag rejects the whole command (the real cause turned out to be 128 - the command is still kept identical to the hand-typed one) | `test_txn_fetch.py::test_commands` - the default command is exactly the hand-typed one (`... all-members DSN -d FOLDER`); any extra flag only when `sources.json` lists it in `extra_args` (global or per source) |
| 128 | Same symptom, real cause: run by hand, `zowe` **prompted for the mainframe password** and then worked; run by the toolkit it could not prompt, so nothing was downloaded - and with stdin attached it could have waited for the full timeout | The Zowe profile stores no password; a background subprocess cannot answer an interactive prompt | `test_session_creds.py` - stdin is closed (a prompt fails at once), the failure message says what zowe wanted and what to do, and Sign in (UI) / `--ask-password` (CLI) pass the password to the subprocess through `ZOWE_OPT_PASSWORD` for this run only - never to disk, the log or a crash file |
| 129 | A specification the size of a book (a heading-less PDF or text file) was ONE section: `docs TERM` could show 260 characters of it, a citation could only point at the whole document, and nothing could hand the model the passage that holds a business rule | Sections came from headings only; there was no command to print a section in full | `test_doc_sections.py` - long sections are cut at paragraph boundaries into parts of about 4,000 characters (`docs.chunk_sections`), each its own citable section; `doc NAME` prints the outline, `--sections n-m` / `--grep TERM` the full text, inside a budget |
| 130 | `--check` (and Fetch) froze after "asking the host for the member list": stages 1-2 fine, the first command that logs on never returned - run by hand the same command asks for the password and works | zowe put up its password prompt; in a background run nobody can answer it and this Zowe version waits instead of failing; the toolkit waited with it (an hour per library on a download) | `test_session_creds.py::test_host_command_hangs_on_the_password_prompt` - host commands get a 120 s "hang" timeout; a timeout with no output and no session password is reported as the waiting prompt, with the two ways to give the password (Sign in / --ask-password, or `zowe config secure`) |
| 131 | With the password supplied, the first logon command still froze; run by hand with the daemon off it asked "Enter the host name of your service" | The plain Zowe CLI (daemon bypassed) does not see the connection profile the user's window uses - the daemon had it; a prompt for the host name inside a background run waits forever | `test_session_creds.py::test_host_command_hangs_on_the_password_prompt` - a host-name prompt, or silence with a session password set, is reported as the missing host with the `extra_args` recipe (`--host`, `--port`, `--reject-unauthorized false`; never the password) |
| 132 | Every legacy file failed with "You cannot call a method on a null-valued expression" right after the interrupt fix | In PowerShell, `Write-Output` inside a function is part of the function's RETURN VALUE: the new PID-report line inside `Get-Word` turned `$w` into a list (line + Word object), so `$w.Documents.Open` ran on nothing. Tested only with the COM path mocked, so the script itself was never run before it reached the user | `test_convert.py::test_office_getters_return_one_object` - no `Write-Output` inside the Office getter functions (static), and the script's own `Report-NewPid` run inside a function must leave the return value a single object (real PowerShell, skipped where absent). PID lines go straight to stdout via `[Console]::Out.WriteLine` |
| 133 | 17 of 21 Word files failed with "Command failed" - the documents live in a OneDrive folder | To Word a file under OneDrive / SharePoint / a share is a cloud document (AutoSave, a web address behind the local path, Protected View for synced files) and an automated Save As to a plain path fails; opening still works, so it looked like a document problem | `test_convert.py::test_office_works_on_a_local_copy_and_the_result_lands_beside_the_original` - every file is copied (data only, no mark-of-the-web) to a local scratch folder, converted there, and the result put back beside the original by an atomic replace; an unreadable source (a placeholder not downloaded) is reported with the OneDrive hint; an old copy is never removed before a whole new one exists |
| 134 | A release of 3 programs and 4 copybooks sits in a lower-environment PDS; the index held only production, so "what changed" - the first question a transition document answers - had no query | The index knew the code as it is, not as it was; and a folder under `estate\` did not name a system unless a manifest said so, so a second copy of a program could not be told from the first | `diff OLD NEW`: changed facts (paragraphs, calls, copybooks, tables, files, fields - shifted fields grouped) then the changed lines of both sides, citable as `[[GC/PGM line]]` / `[[GC-TEST/PGM line]]`; `diff --system GC-TEST` lists the release; the folder between the estate root and the library now names the system, so `estate\GC-TEST\TEST.GC.SRC` is a second environment with no manifest, and its programs expand its own copybooks |
| 135 | Found while testing `diff`: a citation in the `NAME@LIBRARY` form never reached the named library - the gate silently checked the production copy instead | `@` is a legal character in a member name and also the NAME@LIBRARY separator; the greedy name pattern swallowed `@LIBRARY` every time, so `library` was never captured | Non-greedy name in the gate and in `diff`; a member really named `AB@CD` is retried whole when the split finds nothing; a test cites the same line in both libraries and expects PASS on one, FAIL on the other |
| 136 | His QA evidence is 8-9 Excel workbooks with a tab per test area and screenshots pasted on the tabs; asked how that reaches the model, the honest answer was: a tab was a section reading "[37 rows]", the rows were only in the search index with no section number (uncitable), and a screenshot was "image3.png" with no idea which tab it was on | The spreadsheet handler stored rows as a table for search and left the section text as a count; pictures were listed from the media folder, never from the sheet/slide/paragraph that holds them | A tab's text is its rows (`row 12: a | b | c`, the sheet's own numbers), Word/PowerPoint tables likewise, long tabs cut between rows; every picture carries an anchor (sheet / slide / heading) into `doc_image.anchor` and the OCR section heading; `doc NAME` lists pictures by place; `doc --list [FOLDER]` names the indexed documents |
| 137 | Three adversarial probe agents ran real generated .xlsx/.docx/.pptx packages through the new spreadsheet code before it reached him: 12 defects, none visible in the unit tests | Rows without an r attribute were numbered by count, not position; Word content controls (cover pages, template blocks) were skipped entirely; a picture in a heading paragraph was filed under the previous heading; one media file pasted on two tabs kept only the first tab; the chunker split a row when the last row boundary lay in the first half; a cell with CR LF kept a bare CR; a token copied from a report table carried the escaped pipe and failed the gate; scanned-PDF pages were not linked to their OCR section; an index built by the previous toolkit crashed `doc NAME` until rebuilt | All fixed with a regression test each (tests/test_doc_tables.py); the gate unescapes a copied pipe; the queries tell an old index to run the build instead of crashing. The lesson stands: probe with real files before the first field run, not after the first complaint |
| 138 | His first real build: 121,000 members, "4000/121000" on screen after a long time, no way to tell how long the rest would take, whether Ctrl+C would lose the work, or whether the `git pull` he needed for the fixes above would throw the hours away | The progress line had a count and no rate; Ctrl+C had no handler (the traceback left the run half-recorded); the toolkit fingerprint hashed every module, so a pull that only changed a query forced a full re-parse | The 10-second line carries parsed/total, members per second and the time left at that rate; Ctrl+C commits, says how to continue, exits 130, and the next run keeps every parsed member; only the fact-producing modules (FACT_MODULES) are fingerprinted |
| 139 | The overnight build sat at 7,000 of 121,000 members until morning - one member held the whole build, and the old toolkit did not even say which | No limit on how long one member may take; the heartbeat that names a slow member came later, and a parser stuck inside a regular expression cannot be interrupted at all | Each member is parsed on a worker thread with a limit (900 s, `--member-limit`): a timed-out member is recorded as failed with the reason, its half-written facts dropped, the build goes on, and it is not retried until the parser changes; one that cannot be interrupted stops the build with its name and the instruction to move it aside and report its kind, size and line count |
| 140 | Second night: "120706 files read (922 MB) in 220 s - now in System Documentation" and nothing for two hours - the INVENTORY (read + classify + fingerprint every file), not the parse, sat on one file in the documentation folder, with no name on screen and no limit | The per-member limit and heartbeat guarded the parse loop only; the inventory decoded and line-split every non-binary file whole, however big, and a huge text export that looks like code goes through the COBOL line reader | Inventory work runs under the same worker-thread limit (900 s): a slow file is named after 30 s, recorded as failed with the reason and skipped after the limit, an uninterruptible one stops the build with its name; a text file over 32 MB is hashed and classified by its first 8 KB only and is a document, never a member; Ctrl+C during the inventory says nothing was written yet |
| 141 | "In the displays I did not see the MFS lib" - checking why turned up two classifier gaps: an MFS statement with a label in column 1 (`MYFMT    FMT`, `GENDER   DFLD`) did not match the content signature, and a folder called PSBSOURCE, DBDSRC, BMSSRC or MFSSRC matched the generic COBOL rule (SRC / SOURCE) before its own | The MFS signature required the statement at the start of the line; the folder-name rules were tried in an order that put the generic rule first | Label-tolerant MFS signature; FORMAT / FMTLIB / MSGLIB recognised as MFS libraries; the specific library rules (DBD, PSB, BMS, MFS) are tried before SRC / SOURCE; the inventory tick names a folder only every 10 s, so a small library never appears there - the kinds line after the inventory (`cobol=…, mfs=…`) is the check |
| 142 | He caught it: "there is an issue in your logic for the every-10-second status" - the last status line was at 220 s and then two hours of nothing | The "line every 10 s" was checked only when the loop moved to the next file or member, so one slow file silenced it exactly when it was needed; the heartbeat bolted on later started only after the file was read, not while it was being read | The status line has its own clock: a thread prints every 10 s what the loop last counted plus the item in hand and how long it has been in hand (reading X / classifying X / parsing X), with the give-up time once it is slow; the loops only update counters; tests block the loop on one item and require the lines to keep coming |
| 143 | "Again same, stuck on the same file" - a 3.7 MB Word file full of screenshots, which the toolkit processes in a tenth of a second when generated here; the status line named the file but could not say WHERE in the toolkit the build sat | Neither of us could see the position: the status knows the item, not the line of code; a folder that will not list showed as the previous file | After 5 min on one item the build writes every thread's position to `atlas-stuck.txt` (faulthandler: file, line, function - nothing from the estate) and says so; the folder walk reports "listing folder X" / "listing the next folder after X"; `python -m atlas.diag --doc FILE` times one document through the same steps in the foreground with a position dump every 30 s |
| 144 | "I liked the token-saving steps but they will take time to digest - I wish there was an easier way" | Eight tasks in the right order, each output renamed by hand before the next, then a prompt with eight attachments: correct, and too many moving parts for a person under time pressure | `python -m atlas.handover` (VS Code: Atlas: handover pack) gathers every report a transition document needs into one file in the right order, with its size in tokens, and creates the release-facts file beside it; `/atlas-handover` attaches the one file: one request. Building it exposed a real bug: with two copies of a program in the index (GC and GC-DEV), walk and paragraph printed the new copy facts with the OLD copy lines, because source text was looked up by member name alone; origin() now names the copy (SYSTEM/NAME), source lookups and cites follow it, and `cite GC-DEV/PGM` reaches that copy |
| 145 | Third night: the same COBOL member as the first, "whole night", the last status line hours old and no time on it to tell | A parser stuck inside a regular expression holds the whole process: the status clock, the 15-minute limit and the 5-minute stack dump all live in that process and none of them can run; the only sign was silence, and a line without a clock does not show silence | `atlas.supervise` runs the build as a child and watches its output: 180 s of silence means frozen - the child is killed, the member in hand (from `atlas-current.txt`, written before every member) is recorded in `atlas-skip.txt`, the build restarts and records that member as failed with the reason; every status line now starts with the clock; a test freezes a member the way a regex would (no output at all) and requires the supervisor to recover |
| 146 | Three probe agents threw pathological COBOL at the parser to find what held a build for hours: eleven shapes, one root cause behind eight of them, and one exponential regex | The statement scanner rescanned the whole buffered sentence from its start for every line added, so a paragraph with one period at its end (structured style), an unterminated literal, a quote cut at column 72, an EXEC without END-EXEC, an EBCDIC member decoded as Windows text (the EBCDIC blank counted as ASCII), or a 5 MB record with no line ends was quadratic - a 200 KB paragraph already took minutes, a 2 MB one hours; `GO TO name` with no period before the next statement backtracked exponentially inside the regex engine, holding the whole process (the overnight freeze); a dispatcher moving 5,000 names to one CALL variable re-sorted them per CALL and stored 300 MB of candidates; 3,000 COPY statements scanned every expansion run per field | Scanner keeps its position, quote and EXEC state across lines (every character looked at once); GO TO / SORT USING target lists need a real separator; candidate lists sorted once and capped at 40; expansion runs found by bisection; the EBCDIC blank counts for neither side; records with no line ends are cut at 80 - each with a timed regression test |
| 147 | "For 1 hr the last line I see is a control card - it might be on a big file and I cannot tell" | The status line covered only the member loop: after the last file was read (the UTL control-card library is read last) the build compared with the last build, removed the old facts of every changed member, recorded the members, applied the manifest and ran the post passes with nothing on screen; and the removal went member by member through 44 foreign keys with no index plus a full scan of the search index each time - quadratic: 500 members 4 s, 2,000 members 68 s, 121,000 members days, after every `git pull` that changed a parser | One status clock for the whole build and every step announced with the time (`== removing the old facts of N member(s)`), a line when a folder is done (path, files, size, time), a line when each kind of member is done, the member in hand with its size; old facts removed in bulk (a fixed handful of statements) through an index on every foreign key, added to old indexes once: 2,000 members 9 s; a stop during the inventory rolls back and leaves the toolkit change pending; the supervisor never puts a step on the skip list |
| 148 | A second round of three probe agents (218 probes: throughput at 1,500 / 4,500 / 13,500 members, every non-COBOL regex in child processes, the steps around the member loop) measured what still grows faster than the estate | The search index's member column cannot be indexed, so every delete and every `walk` source line scanned all of it; the two post passes looked rows up by a column no index started with (and UPPER() defeats an index on the plain column); line-anchored patterns used `\s`, which with MULTILINE runs across blank lines - the classifier's assembler check was cubic on blank 80-column cards (101 cards: 70 s, holding the process), the IMS stage-1, Easytrieve, FTP and IDCAMS DEFINE scans quadratic; lazy `.*?` scans of sort decks, PDF content streams and HTML ran to the end for every statement that lacked what they looked for | The rowid range of each member's search rows is recorded (fts_span): deletes and line reads are range reads (5 ms / 0.2 ms against 220 / 207 ms at 1.2 M rows), indexes built before are flagged and scanned until a full re-parse clears them; an index on io_op(program_id, target) and on the UPPER() expressions the loops test; `[ \t]` for every line-anchored signature and scanner; tempered scans that stop at the next statement of their kind; a one-pass PDF token scanner; incremental quote counting in JCL continuations; referback DDs by name; the manifest decided per folder - each shape with a timed regression test (tests/test_scale_fixes.py). Forced re-parse of 2,000 members: removal 68 s -> 4 s, whole run = a rebuild |
| 149 | "When the actual build runs, check that errors are displayed properly" - checked by forcing each error on purpose | A FAILED line was printed with the time and the error, but after 121,000 members it had scrolled away hours earlier and the final summary only counted failures; unreadable and oversized files printed no time and were never summarised; a full disk would have failed every remaining member one by one; a crash under the supervisor ended with no supervisor line; the crash report's command line repeated `build.py` for every argument, and the report was written to whatever folder the shell was in | Every problem is printed with the time and, for a parse failure, the toolkit line (`[cobol.py:1234 in _extract_x]`), appended to `atlas-problems.txt` beside the index and listed again at the end, grouped by kind with the first names; an index that cannot be written (disk full, I/O error, read-only, locked) stops the build once, rc 4, with what to do; a crash starts with `BUILD STOPPED BY AN ERROR` in plain words and saves the report beside the index; the supervisor says why the build stopped (crash, cannot write, Ctrl+C) and never restarts into the same error - each case with a fault-injection test (tests/test_build_errors.py) |
| 150 | He asked whether the parser can be improved to close the blind spots and to read the pictures it cannot read today | A diagram pasted into Word, Excel or Visio is stored as a metafile (EMF/WMF), and the Windows OCR engine fails on those outright: they were listed as pictures and never read, and anything under 2 KB was skipped as an icon - which a whole flow chart of text is, in vector form. A multi-page TIFF scan was read as its first page only | `ocr.to_readable_images` draws a metafile onto a white bitmap at twice its size (and each TIFF frame separately) before the engine sees it, with the pages of one scan read as one text; the size floor for vector pictures is 400 bytes; proved on a generated EMF that the engine refuses and its PNG reads cleanly. All of it in ocr.py, which is not a parser, so it costs no re-parse |
| 151 | "Any way to read .mp4 files" - the knowledge-transfer recordings hold what nobody wrote down | Nothing read video. Three traps in doing it with only what Windows ships: the media API refuses a path with forward slashes (what Git Bash passes); the speech recogniser refuses the WAV header Media Foundation writes; and `Recognize()` in a loop stamps every phrase 0:00, so a transcript would put all speech at the start. A build also names a document after the file name minus its LAST extension, so `x.mp4.video.docx` would cite as X.MP4.VIDEO | `atlas.video` (not a parser, no re-parse): frames by `MediaComposition.GetThumbnailAsync`, read by the existing OCR path, one screen written once (a character or two of OCR noise or a clock is the same screen, a typed line is not), dataset names the engine splits (`PROD . CLAIMS`) joined; sound transcoded to 16 kHz mono PCM, the raw samples handed to System.Speech in continuous mode, which keeps one clock; a .vtt/.srt beside the video wins over the recogniser; output `<name>.video.docx` (a .docx, so it is always a document, even when the screen shows `//JOB` or `PROGRAM-ID`), `--out` keeps recordings out of the build's folders. Proved end to end on generated recordings (12 s in the suite, 2.5 and 12 minutes by hand: 28 s for 788 MB), the timestamp fix mutation-checked |
| 152 | "Where is build and rebuild?" - checking the answer before giving it | `atlas.supervise` passed the build's arguments unchanged to every restart, `--rebuild` included: a member freezing at hour ten would have deleted the index and started from zero, while the supervisor printed 'restarts and keeps what was parsed'. Its own test passed `--rebuild` and still passed, because a six-file estate parsed again looks the same; the restart's own line said '0 unchanged and kept, 6 new'. The Ctrl+C message said 'run the same command again', which with `--rebuild` deletes everything too | `supervise.restart_args` drops `--rebuild` from every restart and the restart line says so; `again_hint` makes every stop message say 'WITHOUT --rebuild' when it was given. The test now counts build runs in the index after a restart (a deleted index has one), mutation-checked: passing the arguments unchanged fails it. supervise.py is not a parser: no re-parse |
| 153 | "Review what we have done so far" - a 24-agent review of the eight commits since ef437ff, every finding challenged by a skeptic | Fifteen confirmed. The two that mattered were in the citation gate, untouched for weeks: its name grammar allowed neither a dot nor a space, so every transcript cite (`KT-SESSION.VIDEO`) came back 'unreadable member reference' - the whole recording feature was unusable through the gate - and a document name with a space (every Teams download, most plans) was not recognised as a citation at all and then skipped by the uncited-line check: an answer built on it was CERTIFIED with the claim unchecked. Also: the watchdog passed the build's arguments through unchanged, so a forgotten `--also` removed every document from the index with the line 'gone from disk', `--quiet` made every member look frozen, and `--rebuil` slipped past the no-rebuild-on-restart rule; the PowerShell runner never timed out while a child was silent and hung for ever once a child wrote 4 KB to stderr; a metafile filed on its own had its .ocr.png written into his documents folder and indexed as a new document; members given up on before a restart were named nowhere; a `#` in a member path cuts the skip-list line (build.py, batched) | verify_citations: dots and spaces in names, an unreadable `[[...]]` is a FAIL. supervise: refuses a command that names fewer folders than the index holds (--allow-prune to go ahead), spells out abbreviations, drops --quiet, stops on a second freeze of one member, keeps the problem lines across restarts, never trusts yesterday's atlas-current.txt. ocr: stderr merged, timer kill, conversions under --out, long paths. video: exit codes and stderr become notes, frames never scaled down, caption files on their own, XML-safe text, real folders checked. coverage: a table of failed members, the reason that made a member partial, four more unresolved kinds explained. UI: rebuild asks first. selfcheck names what it skipped. 34 tests added; the build.py items wait for the next re-parse (ROADMAP) |
| 154 | His first OCR run at work: 1,998 pictures with text, 54 empty, 5 failed with `page-0001.png: Exception calling "Wait" with "1" argument(s): "One or more errors occurred."` | The OCR script printed the .NET wrapper (AggregateException) instead of the reason inside it; reproduced here: an empty page file gives exactly that text. Probing further: the engine accepts pictures up to 10,000 px on this Windows but silently downsamples a big one and loses the small print (a 4000x6000 page kept two lines of three), and a page the engine refused was never tried again - `extract_images` skips a PDF once any page is read | The script reports the base exception, says 'empty file (0 bytes)' outright, and reads anything over 2,500 px in overlapping tiles at full resolution (a line repeated across a tile edge is written once); a page rendered earlier but never read is handed to the engine again, and one whose file is empty is rendered again on its own (`-Only` in the PDF script) - so his five are retried on the next run and the message names the cause. ocr.py only: no re-parse |
| 155 | The five failures at work, now named: `page-001-0001.png: empty file (0 bytes)` - five documents, each with a first-page picture of the same scanner-export name | The document's own copy of the picture is empty: the zip entry declares a size and stores no data with a CRC of zero (a broken export - Word shows a red X for it), and Python's zip reader hands back zero bytes without a word, so the size floor passed it, the extractor wrote an empty file, and the engine was asked to read nothing - and asked again on every run | The extractor says it once, with the declared size; the run records the picture as read-and-empty, never hands it to the engine, counts it as `broken`, not failed, and never tries it again. Reproduced with a zip written by hand whose central directory lies |
| 156 | `unreadable: NAME  ([Error 2] No such file or directory: 'out/images\\NAME \\image14.png')` - the document's file name ends with a space before `.docx` | Windows drops a trailing space when it creates a folder, so the folder exists without the space and every path written INSIDE it, spelled with the space, 'does not exist' (proved on 3.12 and 3.14); the member name keeps the space, so `doc NAME` and a citation typed without it missed too | `ocr.folder_name`: no trailing space or dot, no characters a folder cannot hold, no device name, a short hash when the name changed; the document lookups, the images report, `diff`'s member lookup and the gate fall back to the trimmed name. Stripping the stem at inventory is build.py: on the re-parse batch |
| 157 | Five pictures 'empty file (0 bytes)' at work, each the first page of a scanner export - and here a document named with an en dash gave exactly that | The list of picture paths was written as UTF-8 without a BOM and read by Windows PowerShell 5.1 with plain Get-Content, which decodes a BOM-less file in the ANSI code page: an en dash, a curly quote or an accented letter in a document or picture name became a path that does not exist (before 8850d15 that surfaced as the .NET wrapper 'One or more errors occurred'). Then `(Get-Item -LiteralPath $p).Length -eq 0` is TRUE for a path Get-Item cannot find, because $null.Length is 0 in PowerShell - so 'not found' was reported as 'empty'. Found by the probe that tried to break the trailing-space fix | Both list files and the scripts carry a BOM and are read with -Encoding UTF8 (convert.py already did); a path the script cannot see says 'not found on disk'; two documents with one name in two folders no longer share a pictures folder (the second's text was cited under both); `--member`, `images NAME.docx` and a report path typed with a trailing space work; a zip entry named the way Windows refuses is extracted under a safe name. Reproduced with the real command on a document named with an en dash and an accented picture name |
| 158 | The probe that tried to break the trailing-space fix | The gate's name grammar knew only ASCII letters, so a document named with an accented letter (a French title) could not be cited at all - 'unreadable citation', and since LESSONS 153 that rejects the whole answer; a document `PAIR .docx` beside a code member PAIR was never a candidate, because the trimmed-name fallback ran only when the exact name found nothing; a 255-character name with a trailing space made a 256-character folder | `\\w` in the name classes of the gate and of `diff`'s lookup; the trimmed twins are added to the candidates (and to `doc NAME`'s list) instead of replacing them; the folder name is capped before its suffix. The UPPER(TRIM(name)) lookups scan the member table on a miss - an expression index for them is on the re-parse batch |
| 159 | Selfcheck failed on a second machine of his: two OCR tests, 'section 1001 - page 1 not found' although the page had been read | The tests asked the engine for exact words off a rendered page (PREMIUM, STEP020); that machine's OCR pack read the same page a little differently, so WAIVER was found and PREMIUM was not. The toolkit was right; the test was asking for something no two engines promise | A test relies only on the probe word it already checks, or compares the engine with itself (the retried page reads the same as the first time); the run prints the OCR language in use, so the next difference between machines is visible |
| 160 | His first recording at work: 'nothing readable in X.mp4. Cannot open the video: The parameter is incorrect.' | That is all the media layer says when it will not open a file, whatever the cause - a codec the laptop lacks (H.265/HEVC needs an extension a locked laptop may not allow), a download or OneDrive copy that is not all there, a file that is not a video at all | When the clip will not open, the script asks Windows two more questions and reports the answers: the file's own properties (size in pixels and length, or 'no video track'), and whether the media pipeline could transcode it (its FailureReason names CodecNotFound). An empty file is said outright. The note ends with the next step: does it play in the Media Player app - if not, the file or the codec; if yes, send the line |
| 161 | The recording plays in Media Player, and the toolkit still said 'Cannot open the video: The parameter is incorrect' | The frames come from the Windows media EDITOR (MediaComposition), which refuses some recordings that the media PLAYER handles without complaint - a screen recorder's MP4, a Teams download - and says only that. The decoders are the same; the editor is the fussy one | When the editor refuses a recording, it is converted first to a plain MP4 at its own size by the transcoder (the same decoders Media Player uses), with a progress line every 10 s, and the frames and the sound track are read from that; the transcript's note says so. Only when the conversion fails too does the run give up, with what Windows reads the file as. A test hook forces the conversion on the generated recording, since no generated file reproduces the refusal |
| 162 | 'Nothing readable (parameter incorrect)' for most of his recordings, and the next run skipped them as done | An open failure was written down as a 'nothing readable' transcript, and a transcript newer than its recording means 'done' - so the recordings the editor refused were never tried again, not even after the conversion fix landed | A recording nothing could open is not a result: nothing is written and it is tried again on every run; a transcript an earlier toolkit wrote for such a failure is read as 'not done' too, so no --refresh is needed |
| 163 | 'Could this be because the .mp4 are on OneDrive and I selected Free up space?' | Yes: OneDrive leaves a placeholder with the name and the size, Media Player fetches the bytes when it plays, and the media editor refuses the placeholder with 'The parameter is incorrect' - the recordings he had played were downloaded and read, the rest were not | The placeholder attributes (recall on data access / on open / offline) are checked before the media step: the line says it is an online-only OneDrive file and how to download it, the dry run marks them ONLINE-ONLY, and the recording is tried again once the bytes are there. The same attributes already explained an unreadable document in the build |
| 164 | 'That is a big folder, I don't have space to pull all of them on C drive' | Downloading the OneDrive recordings by hand needs the whole folder's space at once | Each online-only recording is downloaded on its own by reading it through (what Media Player does when it plays), with a progress line every 10 s, read, and then handed back to OneDrive (`attrib +U -P`, the 'Free up space' state) - so one recording's space is all the run ever needs. A download that fails is said, and that recording is tried again next time |
| 165 | His first real recording, an hour of people discussing a program over a shared mainframe screen: the transcript held sentences that were never spoken, and his trust in the whole toolkit went with it | The speech recogniser Windows ships (System.Speech, the old dictation engine) was tested here on synthetic speech only, where it read a sentence back nearly word for word; on real meeting audio - several voices, room noise, accents, mainframe words - it produces fluent English nobody said, and a transcript full of it is worse than none. The evidence I accepted was not the evidence that mattered | The recogniser is off unless asked for (`--speech-recogniser`) and its lines are labelled unreliable; without the meeting's own caption file beside the recording the speech is not transcribed and the transcript says so; the caption file (Teams, Stream) is the speech source. Trust check offered: three SCREEN lines against the video at their times |
| 166 | 'Screens don't match either' - his hour-long recording, people discussing a program over a shared mainframe screen | Reproduced here: a 3270 screen shared inside a 720p recording gives the OCR engine 8-pixel characters, and it does not fail quietly - `000100` comes back as `eeeløø`, `WS-RESTART-FLAG` as `KS-RESTART-FLAG`, a name that looks real and is not; inverting or enlarging the picture changes nothing, and even a full-screen emulator at 1080p reads 7 tokens in 10. My earlier tests used 36-point slides: the same mistake as the speech | `atlas.screentext`: every name-shaped piece the engine read is checked against the index (`--db atlas.db` - every program, paragraph, field, dataset, job, screen name), a keyword list and a word list; a misread name one or two characters from exactly one index name is snapped to it (with the engine's O/0, I/1, S/5 confusions folded), an unknown name is marked `?`, junk is `?`, a line with nothing recognisable is dropped, and the transcript says how many lines were kept and dropped. `--keep-frames` saves frames with the text before and after the check. The honest expectation is written down: 1080p full-screen reads mostly, 720p shared window reads in fragments |
| 167 | 1,027 of his programs are partial because a copybook is not in the estate, and he cannot get content off the laptop - 'I have expanded versions of the programs, why don't we use them?' | The expanded version holds the copybook's text inline, and a compiler listing marks every copied line (a C after the line number); an include expander leaves the copybook's name in columns 73-80 or a comment naming it. Nothing read them. And with no way to send a sample, the tool has to tell the format itself and report in numbers | `atlas.recover`: reads the listings the index holds (kind `listing`) and any folder given, detects the format per file, cuts each copied block out, checks it parses as data items or procedure code, compares the copies across programs (REPLACING-free first, then the most common), writes each missing copybook as a marked member under SHARED\\RECOVERED-COPYBOOKS with the program it came from, removes it when the real member arrives, and prints five numbers he can type back. The build's own change detection re-expands the programs that copy a new copybook, so no re-parse. Coverage now says when a scanned document's pictures were read since the build |
| 168 | His dry run: 3,303 listings read, 106 of 136 copybooks recoverable - and three review agents found how the tool could write a WRONG copybook silently, which is worse than a missing one | A ruler searched only in the first 400 lines (a SYSPRINT holds the translator and precompiler output first), a flag other than C dropped from the middle of a block, a COPY after code on the same line or an `EXEC SQL / INCLUDE X / END-EXEC` over three lines never opening a block, a marker-comment block whose guessed end swallowed the program's own fields, one text per copybook written to SHARED when GC and GC-TEST hold different versions, a removed recovered copy leaving the programs that expanded it un-parsed, a REPLACING'd copy assumed post-replacement, 317 listings with nothing flagged and 22 expanded sources with no marks left unread | The ruler is searched everywhere and the offset is proved against the shape of every record; an unknown flag makes the copy suspect; a COPY is found anywhere on the line and an EXEC SQL is gathered to its END-EXEC; a guessed end needs a second program to agree; systems that disagree each get their own copy under their own folder; removal marks the programs for the next build; the REPLACING clause is applied forward to tell library text from replaced text; IBM's `**` after a line number (a statement out of sequence, also `C**`) is read as what it is; coverage warns while a recovered copy still shadows a real member that arrived; and every expanded text is lined up against the original program in the index, so the lines it adds at a COPY are the copybook with no marks at all (a text that differs elsewhere too is not trusted alone). Rejections say what the block looked like, in numbers and a masked line |
| 169 | His second dry run, on the hardened tool: 3,614 listings 'without a readable source column', 0 recovered - where the first version had read 3,303 and found 106 | The new check of the source column judged every numbered line in the listing, and a listing goes on after the source with a data division map and cross-reference tables whose lines carry line numbers too; enough of those and the check failed the whole listing, even with the ruler present. A safeguard added against one wrong copybook silently threw away every right one | The ruler is the compiler's own statement of where the source starts and is trusted; without a ruler the guess is checked on the source lines only, up to the first map or table heading; and the shape check that matters is done on each recovered block on its own, where it still stops a shifted copybook. A test now ends its listing with four thousand map and cross-reference lines |
| 170 | 'In the expanded program, after the program ends, it has other things we don't need to read - the cross-reference of data names and others. What you are complaining about is in those sections' - he opened one listing and found in a minute what three review rounds had not | The tool read the whole listing. After the source, the data division map and the cross-reference tables carry line numbers too, some followed by a letter the tool took for the copy mark, and the copybook names appear again in the COPY/BASIS table: 89,347 'copied lines with no COPY statement before them' and 101 'rejected blocks' were those tables. The one who knows the material found it by looking, once told where | Reading stops at the first heading that opens a section after the source (Data Division Map, Cross-reference, Message code, COPY/BASIS...), a heading being an unnumbered line after twenty numbered ones; a test ends its listing with those tables. And the earlier change stands: only the expanded texts of programs that copy a missing copybook are read at all |
| 171 | His photo of one expanded program, after 'no COPY statements in 986': an Enterprise COBOL 6.3 listing, file named after the program, the COPY on line 718 with its REPLACING continued on the next line, the copied lines flagged C - exactly what the tool expects | Every source page starts with the banner and the ruler again, and the ruler ends with the words 'Map and Cross Reference'. The fix of an hour before, stopping at the first heading that mentions a cross reference, stopped at the first page break of the source: only page one was ever read, and the COPY statements sit on later pages. A fix tested on a one-page listing | A page header - the ruler, the banner - is never the end of the source; the headings that are (Cross-reference OF, Data Division Map, Message code, COPY/BASIS) are matched as such. The test listing now breaks its pages every fifty lines with the banner and ruler repeated, puts the COPY on page three, and keeps the ASA '0' the download left in column 1. His photo did what no review found: it showed the real thing |
| 172 | His untied case, in words: a COPY statement, then the copybook's two hundred leading comment lines, page headers among them - and the tool lost the tie in the middle of the comments. No SQL in the program; 'copy' and 'copybook' are words in the comments | The listing reader closed the open block at EVERY numbered line without the C mark, before it asked whether that line was blank or a comment - so one unmarked blank or comment inside a copied block split it, and every copied line after it was 'untied'. Now a blank or a comment without the mark closes nothing (and is not kept); only a program line does. Also: a COPY statement found while an EXEC SQL gather is still open ends the gather instead of vanishing into it; an INCLUDE whose copied lines print before its END-EXEC is tied to them; the 'Please look' report says WHY the tool had no COPY in hand (the last program line and what closed the block before, as shapes). When the rule is 'close on any unmarked line', ask what an unmarked line can be before trusting it. | tests/test_recover.py: test_a_blank_or_comment_line_printed_inside_a_block_without_the_mark_does_not_split_it, test_a_copy_statement_after_an_exec_sql_that_never_closed_is_still_a_copy, test_an_untied_line_says_why_the_tool_had_no_copy_in_hand |
| 173 | 'If the copybook after the COPY statement is in single quotes, are you handling that?' - no. COPY 'NAME'. is legal COBOL and his shop writes it; the tool's own regression test even used COPY 'PMASTREC'. as the example of 'a COPY the tool cannot read' | Three readers matched a bare name after COPY: the fact parser (no copy_use row, so the copybook was never resolved and never reported missing), the expander (the literal mask blanked the name, so the fields were silently absent and no warning said so) and the recover tool (no COPY seen, so every copied line after it was 'untied' - the 40,797). All three now take a bare or a quoted name, the quote must close, and the keyword itself must sit outside a literal (DISPLAY 'COPY FAILED' stays text). Lesson: a form that a test calls 'unreadable' is a bug with a fixture, not a fixture - ask whether real code is written that way before filing it as unreadable. Costs a full re-parse at work (cobol.py and expand.py are fact modules). | tests/test_fixes.py QuotedCopyNames (parser: quoted name, OF lib, REPLACING, national characters, DISPLAY literal, unclosed quote) and CopybookExpansion.test_a_copybook_name_in_quotes_is_expanded_like_a_bare_one (expander); tests/test_recover.py test_a_copybook_name_in_quotes_ties_the_copied_lines and test_a_commented_out_copy_with_a_quoted_name_is_a_marker (recover) |
| 174 | `ADD WS-I 1 GIVING WS-Y` never recorded WS-Y as written (nor SUBTRACT / MULTIPLY / DIVIDE ... GIVING): `field WS-Y` showed the statement as a read and an impact search for who sets WS-Y missed it; a CALL's RETURNING item was lost the same way | `_ARITH` required TO/FROM/BY/INTO: it captured the operand before the keyword and the target after it and never looked for GIVING, so a statement without the keyword fell through with every identifier a read; nothing read the RETURNING phrase at all. Found by the value-flow review (docs/PLAN-value-flow.md), which needed the pairs to be right before it could walk them. Lesson: a fact table that stores one name per line can be wrong about the MODE and nobody notices, because the name is still there. Costs a full re-parse at work (cobol.py is a fact module) | tests/test_flow.py FlowParser.test_flow_rows_for_each_verb_shape: the GIVING target is a write and not a read on the ADD line, the RETURNING item is a write on the CALL line; test_existing_refs_unchanged pins every other ref of three fixtures to a snapshot |
| 175 | `literal LP` said 'no CALL/LINKAGE/file route found' for a value that reached FLOWRDR through a MOVE, a WRITE and a job | `flow_from` fired for literal setters only, looked at program-owned fields only (`query.py:825-826`) and had no test. The route (MOVE to a record field, WRITE, the job's DD, a sort that keeps the bytes, the reader's copybook) needs per-program offsets and one row per pair a verb moves data between - docs/PLAN-value-flow.md, ROADMAP re-parse batch item 14 | tests/test_flow.py FlowIndex.test_literal_pack_diff_use_flow (FLOWRDR and TEST.STAT.OUT after "Where the value goes", never the old sentence) and test_flow_three_hop_chain_in_order; red until the `flow` query stage lands |
| 176 | 'There are many programs in expanded source where the copybook is defined like this and expanded, but it is in Not in any expanded text' - his line, names changed: `03008   00854  01  XXXX-SEG   COPY  'XXXXX'.` | The listing reader matched only SIX-digit line numbers (Enterprise COBOL, VS COBOL II). The older compilers (OS/VS COBOL, DOS/VS COBOL) number every line they read with FIVE digits and head each page with page number, program, time and date - the header he had shown two days earlier and that I pinned as 'ignored' without asking why its shape differed. Such a listing gave the tool no lines at all. Three review rounds took the fix apart. Round one: widening the patterns made plain sources pass for listings (change logs 'CR 12345', paragraphs '10000-INIT', 'ADD 10000'), an editor COLS comment made the reader skip lines, and a translator listing in front set the column one off. Round two: the column was still GUESSED - one division header, a comment count that border lines, '**' comments and change flags pull one to three columns off; a carriage-control 0 glued to the number read as a six-digit line; a line printed again ended the run; 'INVALID DIVISION' counted as a header. Round three: 'every header agrees' was still a guess - a program writing all of area A in column 9-11 agrees with itself one to three columns off; a six-digit change log made a plain source a listing read at column 1 (so in the committed version too); and an older listing whose column could not be proven was reported as 'not in any expanded text' - the very symptom. Every one of those wrote a copybook shifted or cut, and trusted it. What shipped: the current layout as it was, except that a guessed column must leave room for the line number and a ruler needs its LineID heading; the older layout on its own terms - runs of 20+ five-digit numbers counting up by one (a repeat skipped, a new run where they go down), the record column PROVEN or nothing read: six-digit sequence numbers in columns 1-6 that fit the headers' area A, else comment text lines that agree with the majority of the headers; headers alone prove nothing. A listing seen but not provable gets its own report section, never 'fetch these libraries'. One reading() choice for extract and trace. Lessons: a line of an unfamiliar shape in the data is a sign of an unfamiliar FORMAT, not a line to skip; a recogniser that finds a column must PROVE it and refuse otherwise - a missing copybook is honest, a shifted one is not; and 'not found' must mean not found, not 'not read' | tests/test_recover.py OlderListings (20 tests), PlainSourcesStayPlain (8), Formats test_a_listing_without_a_readable_column_says_so / test_an_unflagged_current_listing_with_a_translator_in_front_lines_up / test_a_translator_listing_before_the_ruler_does_not_end_the_reading, EndToEnd test_an_older_listing_among_current_ones_is_recovered_and_traced / test_an_older_listing_whose_column_cannot_be_proven_is_named_not_missing / test_the_trace_reads_a_text_as_the_reader_does; 24 mutations, one per safeguard, 23 caught by a test - the one not caught is the source-shape filter on candidate columns, kept as defence in depth: no input was found where it decides once the two proofs are in place |
| 177 | His line `01  XXXX-SEG   COPY  'XXXXX'.` (OS/VS COBOL, in many of his programs): the expanded program had no XXXX-SEG at all, the library's own 01 name stood in its place, and every MOVE / READ INTO / DL/I I/O area / CALL USING naming XXXX-SEG pointed at nothing - `field` found no definition and `flow` had no node to start from | expand.py treated the statement like any COPY: the whole program line became a comment and the library text went in unchanged. The OS/VS compiler puts the name written before COPY in place of the library's own 01 / 77 / FD / SD name; nothing modelled that. Now the library's first entry is renamed when it is at that level (a `field_alias` row, the program's `field` row, the line cited in the copybook); a fragment starting at 05 hangs under the program's own 01, kept as code; a COPY on the next line after `01 X` is the same statement | tests/test_fixes.py OsvsLevelCopy (full 01 renamed with alias and copybook origin, bare name through the parser, fragment, 77, FD / SD, `01 X.` then COPY unchanged, COPY on the next line, the build's field / pfield / data_flow / field_alias rows, `layout` of the renamed record by the copybook's name, the program's name and the library's 01 name, with and without --program) |
| 178 | His line `A-100-BEGIN SECTION.  COPY PROCBOOK.` (a SECTION header - or a paragraph name, or any statement ending in a period - then COPY on the same line): the expanded program had no section A-100-BEGIN at all, `PERFORM A-100-BEGIN` pointed at nothing, the copybook's paragraphs were listed under the section before it, and the build's paragraph / perform_edge rows said the same | expand.py echoed the COPY statement's whole first line as a comment (indicator `*`) to keep line accounting, so the program's own text before the COPY keyword went with it; the compiler keeps that text and replaces only the COPY statement. The statement was also gathered from the start of the line, so the prefix's period ended it early and a REPLACING continued on the next line was cut off. Now the OS/VS `01 X COPY Y.` form is decided first (unchanged); any other non-blank text before COPY is emitted live on its own line number with the COPY part blanked (no extra line, the line map unchanged), the continuation lines are the comment echo, and the statement is gathered from the COPY keyword | tests/test_fixes.py PrefixBeforeCopy (SECTION then COPY through the parser: the section with the copybook's paragraphs inside it and the PERFORM resolved; a paragraph name then COPY; `MOVE A TO B.  COPY X.`; `01 X.  COPY Y.` hangs the items under the 01 without a rename; the OS/VS form and a COPY starting its line unchanged; REPLACING continued on the next line after a prefix; the build's paragraph and perform_edge rows) |
| 179 | His `zowe zos-files download all-members "DSN" -d folder` never asks for anything in his window - he had said so - yet run by the toolkit it asked for a password, the hint text told him to type one, and the UI he was given did not work | Two things the toolkit changed between his window and the subprocess: it forced `ZOWE_USE_DAEMON=no`, so the plain CLI had to find the team configuration itself; and it ran from the toolkit's folder, while Zowe looks for a project `zowe.config.json` from the CURRENT folder upward - from the wrong folder it finds no profile and asks for host / user / password. The password was never the problem; a window command that works without a prompt proves the configuration holds everything zowe needs. Lesson: when a command works by hand and not from the tool, reproduce the hand run exactly - same words, same folder, same environment - before adding anything; a prompt the window never shows is a difference the tool introduced, not a secret to collect | tests/test_zowe_window.py: HisCommand (the download is `[zowe, zos-files, download, all-members, DSN, -d, folder]` word for word, profile / user / password flags only when set, `session_env` leaves ZOWE_USE_DAEMON untouched by default and sets no only with daemon off), WorkingFolder (every subprocess runs with cwd = the working folder, default the folder of sources.json; a missing folder is named; an old sources.json without the keys loads with the defaults and the in-memory marker never reaches the file), CheckReportsTheConfiguration (`zowe config list --locations --root` from the folder, paths only, never a host or user value, the words for none, a Zowe without the command), PlanShowsCommandAndFolder (--plan and the fetch log), TheFirstHintIsTheFolder (folder and daemon before `zowe config secure`, `--ask-password` and `extra_args` in every hint), UiRoundTrip (the field and the checkbox saved and read back, Plan redacted; skipped without Tk) |
| 180 | After the OCR run at work the coverage report still said 'doc 92 - no extractable text, likely a scan, needs OCR': the same 92 documents before and after the run | atlas.ocr stored the recognised text as sections of the document (ordinals 1001+) and never touched the document's parse_status, so a scan OCR had read stayed 'partial' for ever; coverage footnoted 'pictures read by OCR since the build' but counted it as parsed only in part. Now mark_read_documents() runs at the end of every OCR run over every document: partial for 'no extractable text' + OCR sections present -> ok, note 'text read by OCR (N pictures, sections 1001+)'. Lesson: a tool that adds what was missing must also retract the note that said it was missing | tests/test_ocr_images.py ReadScansCountAsParsed (a PDF that draws nothing: partial before, ok after the pass, not counted twice, coverage drops the doc row) |
| 181 | wrong fact | His coverage report said 701 COBOL programs 'parsed only in part' while its own 'Copybooks not found' table held ~60 names (at most 172 programs); the libraries fetched lately hold same-named copybooks with different content across systems | build.make_resolver picks one copy (COPY..OF > same system in declared order > authoritative > same folder > first), records an 'ambiguous_copybook' row AND returns the note; expand.py repeats it as a COPY warning, and index_cobol stores every expander warning as an 'expand' note and marks the member partial when any exists - so a program that expanded COMPLETELY with a chosen copy got the same word as one whose copybook is missing. The build side cannot change without a night's re-parse (ROADMAP re-parse item 18), so the query side tells the two apart: partial_kind() / is_truly_partial() read the resolver's wording (' with different content; used ') in the member's 'expand' notes; coverage prints such members under 'Complete, with a copybook chosen among several' with the copy used, keeps the true partials (NOT FOUND, skipped - recursive, nesting) with the reason that MADE them partial, and shows that the two counts add up to the stored partial count; program / pack read 'parse: complete (copybook chosen among several - see notes)'; flow's '[program partial: ...]' and 'Unresolved in scope' no longer repeat the choice as a gap. Lesson: one status word for two different facts is a wrong fact; a note that records a decision must not be stored as a note that records a gap | tests/test_chosen_copybook.py ChosenCopybookIsNotPartial (the build unchanged: the chosen program is 'partial' in the db with the resolver's wording in its 'expand' note; partial_kind / is_truly_partial / parse_label for chosen, missing and plain; coverage's two tables, the reason that MADE the missing one partial, --all, the sum line and the unresolved-by-kind split; program and pack headers with the note kept; flow marks only the truly partial program) |
| 182 | wrong fact | ~500 of his programs carry an 'ambiguous_copybook' row: the copybook's name exists in several libraries with different content and the build's resolver GUESSED which copy to expand (COPY..OF > same system in declared order > authoritative > same folder > first) - every field, offset and flow answer for such a program rests on that guess; his compiler listings end with a table naming, per copybook, the DD name and the LIBRARY DATASET the compiler read it from, which nothing read | The resolver had no way to know what the compiler did; the listing says it. Now atlas.recover parses the listing's copybook-source table by SHAPE (a member, a DD name of 1-8 characters, a dotted dataset, then only numbers and dates; a numbered source line is never a row; no heading text is relied on), reads the listing of every program with such a row as well as the programs that copy a missing copybook, stores the rows per program in its own table `listing_copy_source` (replaced each run; no fact table changes - the build's use of it is ROADMAP re-parse item 19) and checks every choice: the chosen member's dataset (the `library` table, else the folder named after its dataset) against the listing's - CONFIRMED, CONTRADICTED (the report names each one with the library the listing says and whether the index holds that copy), UNKNOWN (no listing, no table, no row, dataset not known). `coverage` says how many of the choices are confirmed / contradicted / unknown; `program` prints "listing says: COPYBOOK came from DATASET (DD)" and whether it agrees with the copy used; `copybook` prints the datasets the listings name per program count. Lesson: when the estate holds the compiler's own record of a decision the tool had to guess, read the record and check the guess - never let a guess stand unverified while its proof sits in the same folder | tests/test_copy_sources.py CopySourcesParser (his layout with carriage control and a member in parentheses, headings and near-misses ignored, numbered source lines never rows, older and current listings without a table give nothing), ChoicesCheckedAgainstTheListings (confirmed / contradicted through the folder name, through the `library` table and for a library not held / unknown for no table and no listing; the console line, the report section with every contradicted row, the stored rows replaced on a second run, dry run stores nothing, coverage / program / copybook lines, nothing said before recover has run, parse_status and the rows untouched), CheckedWhileRecovering (the recovery path checks the choice on the same read) |
| 183 | His coverage report still said 'COPY PROCBOOK NOT FOUND' for a program after he fetched the library that holds PROCBOOK into `estate\SHARED\<DATASET>\` (members arrive as .txt) and built twice - once after the fetch, once after atlas.recover had removed 145 recovered copies; PROCBOOK is a PROCEDURE copybook (paragraph names and statements, no level numbers, no DIVISION header) copied on his `A-100-BEGIN SECTION.  COPY PROCBOOK.` line (LESSONS 178), and the member was on disk and in the index the whole time | Two build-side mechanisms, neither of which can change without a night's re-parse (ROADMAP re-parse items 20 and 21). (1) A text with no content signature - no level numbers, no IDENTIFICATION DIVISION, no JCL - is typed by its FOLDER NAME (classify.DIR_HINTS): PROCLIB / PROCS make it a proc, CNTL / CARDLIB a control card, a plain folder a document, and a dataset-named folder with no hint 'unknown' (then the kind declared in sources.json for that library, when there is one - a hand-downloaded folder has none). The build's resolver expands only copybook / cobol / sql / unknown, so a procedure copybook filed as proc, ctlcard or doc is NEVER found, while one filed 'unknown' IS found - but only when the program is parsed again. (2) The build re-parses the programs that copy a NEW member only when that member's kind is copybook or cobol (`changed_names`); a new member of kind unknown or sql forces nothing, so every program that copied it stayed 'partial - COPY X NOT FOUND'. atlas.recover marks programs pending only when it REMOVES a recovered copy, and a procedure copybook is never written as one (looks_like_copybook rejects it), so nothing was removed and nothing marked; and missing_copybooks() excludes a name a member of a resolver kind carries, so the arrived case fell through every report silently - while the misfiled one was counted as missing and, with listings present, rejected as 'not a copybook the parser can read' without a word that the member was on disk. Now, query side and recover side, no re-parse: `atlas.recover` scans, before its missing-copybook count, every unresolved COPY whose copybook's name the index holds - a member of a kind the resolver expands means the program was parsed before it arrived: those programs are marked pending by member id (not on --dry-run), the console says '  N program(s) copy a copybook that has arrived since they were parsed (K copybooks): marked for the next build', the report gains '## Copybooks that arrived after the program was parsed' (copybook, kind, folder, programs) before '## Written' and the 'next: run your usual build command' line is printed; a name whose only members are of a kind the build never expands (proc, ctlcard, doc, jcl, listing ...) is reported, not marked, under '## A member with the copybook's name exists but is filed as something else' with the fix in words (the folder name decided the kind: rename the folder to end in COPYLIB, or declare the library's kind in the UI's table and run the build); run() returns 'arrived', 'misfiled' and 'marked' in its stats, and a run that ends early (nothing missing, no listings) still writes those sections. `coverage`'s 'Copybooks not found' table gains the column 'a member with this name exists?' ('-' / 'yes: FOLDER (KIND) - parsed before it arrived: run recover, then the build' / 'yes: FOLDER, filed as KIND - not a kind the build expands: rename the folder to end in COPYLIB or declare its kind in the UI, then build') with a note under it, and its partial-members note counts both; `program`'s NOT FOUND cell carries the same note; `copybook` says 'NOT FOUND as a copybook - a member with this name exists, filed as KIND (folder F)' with the fix instead of a bare NOT FOUND. Lesson: 'not found' and 'found but not usable' are two facts; a report that says the first while the file sits in the folder it was asked to look in sends him fetching what he already has | tests/test_arrived_copybooks.py ArrivedAfterTheParse (the program parsed while PROCBOOK is absent; PROCBOOK.txt lands in a dataset-named folder with no COPY hint -> kind unknown; the incremental build leaves the program partial - asserted, so the test documents re-parse item 21; coverage's column and program's cell say 'parsed before it arrived'; a dry run reports and marks nothing; the run marks it, the console line, the report section, stats, the 'next' line; the next incremental build makes it ok with the section's paragraphs present and the not-found table empty; a program whose own copy resolved is left alone; members_named / arrived_copybooks / same_named_note), FiledAsAnotherKind (PROCBOOK.txt in a PROCS folder -> proc: the console line, the misfiled section with the fix sentence, nothing marked, another build changes nothing; coverage's column, program's cell and copybook's branch say 'filed as proc'; the folder renamed to end in COPYLIB makes the program whole), DataCopybookIsTheControl (a data copybook in the same folder is a copybook by its level numbers, the existing forcing re-parses its copier, the new step reports nothing) |
| 184 | On the verifier's control estate with nothing wrong - program CLMUPD02 whole, copybook CLMEDITS copying CLMSUB, both in a COPYLIB folder, every link resolved - `atlas.recover` said '1 program(s) copy a copybook that has arrived since they were parsed (1 copybook): marked for the next build', listed `\| CLMSUB \| copybook \| PROD.CLM.COPYLIB \| CLMEDITS (copybook) \|` and marked the COPYBOOK pending; the next build re-inserted it under a new member id and nulled the program's link to it (the program was not parsed again), so `coverage` then listed both copybooks under 'Copybooks not found' with 'parsed before it arrived: run recover, then the build', `program` said NOT FOUND for a whole program, and each recover/build pair alternated the damage without settling. On his estate every DCLGEN and record sub-layout copied from inside another copybook is such a row: hundreds 'arrived' on the first run, then the links of every program copying them gone - the symptom he reported, multiplied | index_copybook records a copybook's own COPY statements in copy_use with NO resolved_member_id (it parses a copybook for copies only and never resolves them), so a copybook member's rows are NULL for ever; arrived_copybooks() keyed on `resolved_member_id IS NULL` over every copier kind and _mark_members marked whatever it found - and coverage's 'Copybooks not found' table had read the same rows since it existed (`\| CLMSUB \| 1 \|` on a whole estate: a wrong fact the new column turned into a false instruction). Every copier in the tests was a program. Now only a PROGRAM's row says whether a COPY was found: arrived_copybooks(), coverage's table and its partial-members note read copiers of kind cobol, and nothing marks a copybook member pending (_mark_members and _mark_programs mark programs only: a program carries its own row for every nested COPY, so that is enough). Wrong words fixed on the same round: 'run recover, then the build' was not the whole fix for a member typed 'unknown' - the build has no parser for that kind, so its own lines are not in src_fts and `paragraph` printed empty Source lines for them - so the arrived note (coverage, program, copybook), recover's console and report, README, the FieldManual and ROADMAP 20/21 add 'and rename the folder to end in COPYLIB (or declare the library's kind in the UI's table) so the copybook's own lines are indexed and citable'; the partial-members note prints only the clause whose count is not zero (it told him to run recover for 0 copybooks); the coverage column pairs each folder with its kind ('downloads, filed as doc; PROD.CLM.PROCS, filed as proc') and the report's two columns keep the same order; the misfiled console line carries the fix and its own 'next:' line instead of sending him to the listings; `copybook NAME` says why a program still says NOT FOUND for a member it lists; a run with nothing left to say replaces the earlier run's work\recover.md instead of leaving it saying 'marked for the next build'; and row 183's 'missing_copybooks() excludes the case' is corrected - it left out the arrived case only. Lesson: a NULL the build never fills is not a fact about anything; before a scan keys on a column, ask which writer sets it | tests/test_arrived_copybooks.py NestedCopybookIsNotArrived (both copybooks present, program whole: recover reports nothing, marks nothing, every member id and link stays through two recover/build rounds, the not-found table has no row for the nested copybook), NestedCopybookArrivesLater (the program carries its own row for the nested COPY: recover marks the program only, the copybook keeps its id and status, the next build makes the program whole), ArrivedAfterTheParse (the folder clause in every note, the 'unknown' member's lines absent from src_fts, `copybook` says why, no zero-count clause, the stale report replaced), FiledAsAnotherKind (the console fix line and its 'next:', no zero-count clause, two misfiled copies paired folder-to-kind in coverage / program / copybook / the report) |
| 185 | On the verifier's estate with nothing wrong - RECPGM copies RECBOOK on his `A-100-BEGIN SECTION.  COPY RECBOOK.` line and RECBOOK copies itself, both in a COPYLIB folder from the first build (and a chain N01..N13 nested one level deeper than the expander follows) - `atlas.recover` said '1 program(s) copy a copybook that has arrived since they were parsed (1 copybook): marked for the next build' on EVERY run, coverage's 'Copybooks not found' table listed `\| RECBOOK \| 1 \| yes: PROD.GC.COPYLIB (copybook) - parsed before it arrived: run recover, then the build \|`, `program RECPGM`'s cell and `copybook RECBOOK` said the same, each build parsed the program again and its row stayed NULL: it never settled; and 'missing copybooks in the index: 1' sent him to the listings for a member the index held. Before this batch the table's `\| RECBOOK \| 1 \|` was the wrong fact; the new column made it an instruction | expand.py writes a program's copy_use row with no resolved_member_id for three things - a system include (excluded by name), a COPY it SKIPPED (a copybook copying itself: 'recursive'; nesting deeper than MAX_DEPTH = 12: the name is never looked up) and a COPY NOT FOUND - and row 184's own lesson, 'ask which writer sets the column', was applied to index_copybook's writer only. A skipped COPY's member exists and the program was parsed after it; the reason is in the program's own 'expand' note, the one place the build wrote it. Now recover.skipped_copies() reads those notes and a NULL program row counts as 'not found' only when it is not a skipped one (a name the same program also reports NOT FOUND at another COPY stays not found): arrived_copybooks() leaves such rows out, coverage's table does not count them and says the skip under it, `program`'s cell and `copybook` say 'COPY skipped - recursive' with no instruction, and missing_copybooks() no longer counts a copybook copying its own name as missing (it is the member). On the same round: `copybook` counts programs, not COPY sites (a program with two COPY lines of one book read '2 of the programs above'); a run whose only work was removing recovered copies and marking their programs wrote 'Nothing to report on this run' over the report and printed no 'next:' line, while the console had just said the programs were marked - the report now names the removed copies and the marked programs, stats 'marked' counts them, and the 'next:' line follows the console; a run whose every missing name is a misfiled member says the folder fix comes first instead of sending him to the listings; row 184's `work\recover.md` had lost its backslash to a carriage-return byte and the FieldManual's `work\fetch-list.txt` to a form feed - a non-raw string, in both. Lesson: the same lesson, applied to every writer this time - a column three writers set to one value carries three facts, and the note beside it is the only thing that tells them apart | tests/test_arrived_copybooks.py RecursiveCopyIsNotArrived and DeepNestingIsNotArrived (skipped_copies, arrived_copybooks and missing_copybooks empty; the not-found table without the row and with the skip said under it; the program cell and `copybook` say the skip and never NOT FOUND; no note says 'parsed before it arrived'; two recover/build rounds mark nothing and change no id or status; the resolved row and the partial reason kept), NotFoundWinsOverSkipped (one name NOT FOUND at one COPY and skipped at another stays not found), RemovedCopiesAreNotNothingToReport (the report section, the 'next:' line, stats), ArrivedAfterTheParse.test_copybook_counts_programs_not_copy_sites, FiledAsAnotherKind (the qualifying line, no listings 'next:' when the only missing names are misfiled) |
| 186 | His copybook sat in a folder whose name ends in `.COPYLIB`, yet `copybook NAME` said NOT FOUND and `program NAME` said 'Not a program: indexed as a `asm` member'; every program copying it was 'partial - COPY NAME NOT FOUND' while the file was in the estate, and the misfiled note of row 183 told him to rename the folder to end in COPYLIB or declare the library's kind - the folder already ended in COPYLIB, and neither changes anything. Data copybooks with a START-DATE field are common in insurance code: probably a large share of his 'Copybooks not found' table | classify.py checks the content signatures before the level-number signature and before the folder hint, and `_SIG_ASM` (`^(?:[ \t]*\w+)?[ \t]+(CSECT\|DSECT\|START\|DFHEIENT)\b`) fires on any line whose first or second word BEGINS with START: `05 START-DATE PIC X(8).`, `PERFORM START-PARA.`, `MOVE START-DATE TO WS-DATE` -> ('asm', 'CSECT/DSECT'); the same text without START is a copybook. Two sibling weak signatures do the same: `_SIG_LISTING` on a comment saying MODULE MAP or CROSS REFERENCE TABLE (-> listing), `_SIG_MFS` on a line whose first word is MSG / FMT / DEV / DFLD / MFLD (-> mfs). The resolver expands only copybook / cobol / sql / unknown, so the member is never found; and the declared kind in sources.json only replaces 'unknown' (build.py `_inventory_one`), so the 'declare the library's kind' half of row 183's advice was false for every member a signature had typed. The classifier is a fact module: the fix is ROADMAP re-parse item 22. Now, query side and recover side, no re-parse: `recover.how_classified()` runs `classify.classify()` again over the member's first 8 KB and reads its REASON - a content signature ('CSECT/DSECT', 'MFS statement', 'compiler listing ...'), the library folder, or the extension - and quotes the line and the word that fired; `refile_misfiled()` (the stand-in for item 22, not on --dry-run) sets `kind='copybook', parse_status='skipped', parse_error='re-filed as copybook by atlas.recover on DATE: ...'` on a member typed asm / listing / mfs by its content that sits in a folder classify.DIR_HINTS maps to copybook or carries level numbers, holds no JCL line and no IDENTIFICATION DIVISION, and has none of the real shapes (a label in column 1 before CSECT / DSECT, START alone or with a numeric operand, DS / DC with a type; a listing's banner or numbered lines; a labelled MFS macro or TYPE= / POS= operands), re-hashes it as the build hashes a copybook, drops the rows its parse as the other kind wrote, and marks the copying programs by name; the build keeps the stored kind of an unchanged, settled member and the expander reads the copybook's text from disk, so the next incremental build makes the programs whole (the member's own field rows wait for the re-parse - said in every report). Console '  N misfiled copybook(s) re-filed as copybook in the index (the classifier had read them as asm by a line of their text): M program(s) marked for the next build', report '## Re-filed as copybook' (copybook \| had been filed as \| why \| folder \| programs), stats 'refiled'; the misfiled table gains 'why that kind' ('filed as asm by its content (line 2 `START-DATE` reads as Assembler ...)' against 'filed as proc by its folder (the folder name ends in PROCS)') and its 'what to do' keeps the rename sentence for the folder-decided only - 'no folder change helps' with the re-file, or the honest reason, for the content-decided (a real Assembler member with the copybook's name is not re-filed: the copybook is another member, still to fetch; a copy elsewhere or under another name reads the same, so no such advice is given); `coverage` / `program` / `copybook` say 'filed as asm by its content (...) - no folder change helps: run `python -m atlas.recover --db atlas.db`, which re-files it, then the build'; a re-filed member reads 'indexed as a copybook (re-filed by atlas.recover; its own layout rows arrive with the next full re-parse)' and coverage counts it apart from the skipped members; a --rebuild files it as asm again until item 22 and recover re-files it again; so does any build that re-parses every member - the manifest changed (a library added or re-kinded in the UI's table rewrites it) or a parser module changed - and recover after such a build re-files it again (row 188). Lesson: an instruction that names a fix must be checked against the mechanism that decided - 'rename the folder' was written for the folder hint and repeated for a member the folder hint never reached | tests/test_refiled_copybooks.py DataCopybookWithStartDate (the build files `05 START-DATE` as asm - asserted, so the test documents item 22; every report says by its content with the recover instruction and no rename; a dry run re-files nothing and says what it would; the run re-files, marks, the console line, the section, stats; the incremental build resolves the program with START-DATE at offset 5 in pfield, the member kept as a copybook, the copybook's own field rows absent; a further run says nothing), ProcedureCopybookWithStartPara (PERFORM START-PARA on his `A-100-BEGIN SECTION.  COPY PROCBOOK.` line: the section and the copybook's paragraphs after the build), ListingAndMfsSignatures (MODULE MAP -> listing, a section named MSG -> mfs, both re-filed in one run, the MFS parser's rows gone), GenuineAssemblerIsNotRefiled (a CSECT in a COPYLIB folder: reported by its content, not re-filed, the honest sentence, the listings path left open), ProcedureCopybookInProcsFolder (the rename sentence unchanged, 'by its folder' in the report, a doc by its extension), RebuildRefilesAgain, TheVerdict (what is re-filed and what is not, line by line: the COBOL START verb is, a labelled START / DS / DC, an MFS TYPE=, a listing banner, a JCL line, a program and a file changed since the build are not) |
| 187 | The verifier's estates for row 186, one round later: (1) three real Assembler members in a COPYLIB folder - `PLLONGLABEL CSECT` with only `DC A(TABLE)` / `DC AL2(5)`, an unlabelled `CSECT` with USING / LA / BR, `PLHEX START X'100'` - were judged 'no Assembler shape', re-filed as copybooks, and their copiers went 'ok' with Assembler text expanded as COBOL (a wrong fact where the truth was 'partial - COPY NOT FOUND'); (2) a procedure copybook with `PERFORM START-PARA` in a dataset-named folder with no COPY hint (his hand-fetched libraries, row 183) got a cell reading 'no folder change helps (the content decided); not re-filed: ... a folder ending in COPYLIB would let this tool re-file it' - a sentence that contradicts itself - while the console sent him to the listings; (3) a member typed by the kind declared for its library in the UI's table read 'the file now reads as unknown ... run the build first', which the build would not change; (4) a second recover run before the build called the member re-filed a run earlier 'a copybook that has arrived since they were parsed' over the previous run's 'Re-filed as copybook'; (5) a re-filed member whose text changes on disk before the re-parse is filed as before again by the next build, which un-links its programs without parsing them again - 'ok' with the old text's fields until recover and the build run again (build side) | (1) `_ASM_SHAPE` allowed a label of 1-8 characters only before CSECT / DSECT (HLASM labels run to 63), no unlabelled CSECT, START with a numeric operand only, and DS / DC types without the address constants A / V / S / Q: a guard written from the fixtures' one Assembler member, not from the shapes real members take. (2) `content_fix()` printed one prefix for every refusal while `refile_verdict()`'s own words named the folder fix. (3) `how_classified()` read every difference between the classifier's answer and the stored kind as 'the file changed since the build'; the build's step after the classifier - 'unknown' becomes the declared kind (`_inventory_one`) - was not a case. (4) `arrived_copybooks()` counted a re-filed member (kind copybook now) among the accepted members like any other. Now: `_ASM_SHAPE` takes a label of any length or none before the exact word CSECT / DSECT, START alone or with a numeric or quoted operand (a remark may follow) or one bare symbol ending the line (never a hyphenated COBOL name), DS / DC with the address constants included, `USING *`, `EQU *`, `BR 14` - erring towards refusing: a COBOL `START CUSTFILE` alone on its line is left for item 22 with the honest sentence; a reading this tool refuses only for want of a COPYLIB folder (`folder_helps`) gets 'rename the folder to end in COPYLIB, run recover again - it then re-files the member - then the build' in every cell, its own console line, its own 'next:' and a place before the listings, and coverage's partial-members clause counts it apart; `how_classified()` takes the member's stored sha256 (`member_readings(found, conn)`) and, for an unchanged file the classifier reads 'unknown' while the index holds a declarable kind, says 'by its declared kind' with 'declare it copybook there' (`DECLARED_FIX`, `DECLARED_CELL`) - 'changed' only when the bytes differ; `arrival_scan()` keeps a re-filed name whose programs are marked already in a third list, `waiting` (console 'N copybook(s) re-filed on an earlier run wait for the build', report 'Re-filed on an earlier run - waiting for the build', stats 'waiting', coverage's clause), and `arrived_copybooks()` returns the other two unchanged; (5) is said in the report's 'Re-filed as copybook' section and in ROADMAP 22. Lesson: a guard proven only on the fixture that motivated it is unproven - list the shapes the real thing takes before writing the regex; and a sentence built from two clauses written apart must be read whole once, because each half was true alone | tests/test_refiled_copybooks.py RealAssemblerShapesAreNotRefiled (the three shapes refused with the honest sentence, the copiers partial after the build, nothing expanded), NoHintFolderStartPara (every cell, the console, the 'next:', no listings line; the folder renamed, the build, the second run re-files, the build makes the program whole with START-PARA in the section), DeclaredKindDecided (a manifest kinds entry: 'by its declared kind' in the report, coverage, program and copybook, never 'run the build first'; declared copybook, the build makes it whole), SecondRunBeforeTheBuild (waiting, not arrived, in the console, the report, stats and coverage; nothing after the build), TheDocsSayIt, TheVerdict (the shapes line by line, the COBOL START verb with and without a hyphen, folder_helps / content_fix, how_classified with a matching, a differing and no sha) |
| 188 | The verifier's estates for rows 186-187, a round later: (1) the report's 'Re-filed as copybook' section, ROADMAP 22 and row 186 all named `--rebuild` as the one build that undoes a re-file - on a scratch estate an UNRELATED library declared in the manifest after a re-file made the build say 'manifest changed since the last build: every member is re-parsed' and file STARTBK asm again, the program partial and its COPY row NULL; his routine (fetch a library, add it in the UI's table, build - row 183) rewrites manifest.json on every build, so it reverts every re-filed copybook on each fetch until recover runs again; (2) a name carried by a re-filable COPYLIB copy AND a folder-typed PROCS copy: `--dry-run` printed '1 misfiled copybook(s) would be re-filed' and then, for the same name, 'exist in the index only as a member of a kind the build does not expand ... until the folder is renamed', 'the folder fix comes first' and 'next: rename the folder(s)' - no folder change was needed: the run without --dry-run re-files the COPYLIB copy, leaves the PROCS copy alone, and the build resolves the program; (3) the window row 187 (5) discloses had no word on the query side: after a re-filed copybook's text changed on disk and a build, `program STPGM` printed 'parse: ok' in its header and '**NOT FOUND**' in its COPY table on the same page, and coverage's 'Members parsed only in part' said _none_ | (1) the stored kind survives only while build.py keeps every unchanged member's kind (`not force_all`), and force_all is set for three reasons, not one: `--rebuild`, the toolkit fingerprint changed (a parser module), the manifest sha changed - and atlas/ui.py writes the manifest from the sources table on every build (fetch.write_manifest: authoritative, systems, system_of, copylib_order, kinds); (2) on a dry run `refile_misfiled()` leaves the would-be-refiled entry in `misfiled`, `folder_decided()` is true through its PROCS reading, and `folder_fix` in `run()` took every folder-decided entry - the real run drops the entry from `misfiled`, so only the dry run said it; (3) the build sets a program's copy_use row to NULL when the member it had expanded leaves the index (`_forget_member`: the file changed, or went) and parses the program again only when the new text is filed copybook or cobol - a NULL row on an 'ok' program was a state no query named. Now: the section, ROADMAP 22, row 186, README and the Field Manual ADD, after the pinned --rebuild sentence, 'so does any build that re-parses every member - the manifest changed (a library added or re-kinded in the UI's table rewrites it) or a parser module changed: run this tool after such a build and it re-files them again' (`recover.REPARSE_UNDOES`); the dry run leaves a would-be re-filed entry out of `folder_fix` as `by_content` already did, so its console says 'run without --dry-run first' and never 'next: rename'; `recover.unlinked_ok_programs()` names every 'ok' program with a COPY row no member resolves (not a system include, not a skipped COPY), `program` says beside 'parse: ok' ', but 1 COPY row is unresolved (STARTBK): the member it had expanded went out of the index since - its text changed on disk and the classifier filed the new text as another kind, or the file went - and nothing parsed the program again, so its fields are those of the earlier read of that copybook; run `python -m atlas.recover --db atlas.db`, then the build' (`query.unlinked_ok_note`), and coverage adds under 'Members parsed only in part' 'Not counted above: N program(s) marked `ok` ...' (`query.unlinked_ok_clause`) - the same run of recover re-files the new member and marks the program by name, and the build makes it whole. Lesson: a sentence that names ONE trigger for a mechanism must be checked against the code that sets the flag - `force_all` had three, and the one he hits on every fetch was the one not named | tests/test_refiled_copybooks.py ManifestChangeRefilesAgain (an unrelated library declared in the manifest after the re-file: asm again, the program partial, recover re-files, the build makes it whole; the sentence pinned in the report after the --rebuild one), DryRunWithAFolderTypedCopyToo (the COPYLIB and the PROCS copy: the dry run says 'run without --dry-run first' and no folder line, the run re-files the one copy and leaves the other, the build resolves the program to the copybook member), OkWithAnUnlinkedCopyRow (the text changed after the re-file and the build: 'ok' with a NULL row and the old fields, said by `program` and by coverage, a system include never counted; recover and the build heal it with ST-NEW at offset 16 and nothing is said after), TheDocsSayIt (ROADMAP 22, rows 186 and 188, README and the Field Manual carry the clause) |
| 189 | After the re-file shipped he ran recover on his estate: no 're-filed as copybook' line, no 'marked for the next build' line, and he asked 'what is ASM shape?' - the report had refused his copybook with 'it has the shape of an Assembler member'. His procedure copybook (copied on the `A-100-BEGIN SECTION.  COPY X.` line) holds the COBOL START verb the way it is usually written: `START CUSTFILE` on a line of its own, `KEY IS >= ...` or `INVALID KEY ...` on the next | The first shape guard (row 187) counted `START` with one bare symbol on the line as the Assembler statement, to err towards refusing; an unlabelled `START SYMBOL` is exactly the COBOL verb with its clause on the next line, and its own test pinned the refusal as 'the price of erring towards refusing'. A price paid on his estate, not on a fixture. Also: a strong signature (a BMS macro name, a REXX header, a DBD macro) fired by a COMMENT line refused a re-file although the code lines carry nothing of that kind | `recover._ASM_SHAPE`: START is Assembler only with a label in column 1 (a COBOL line never begins there; the Assembler START names its control section) or with a numeric or quoted operand; unlabelled START with a bare symbol, or alone, is the COBOL verb. `recover._signature_only_in_comments()`: a strong signature that fires on the whole text and on none of its code lines (`reader.read_cobol_lines`, comment lines dropped) came from a comment and no longer refuses. Rule for me: when a guard errs towards refusing on a shape the estate uses every day, the honest sentence in the report is not enough - the user reads 'ASM shape' and cannot act on it; the guard must know the COBOL shape too | `test_refiled_copybooks.py` TheVerdict (`START CUSTFILE` alone and with INVALID KEY re-filed; `PGM START`, `START 0`, `PGM START SYM` refused; DFHMDF in a comment re-filed, on a code line refused), ProcedureCopybookWithTheStartVerb (case J: the whole cycle) |
| 190 | His real run with the listings (2,834 read): 61,909 copied lines 'could not be tied to a COPY statement', 24 missing copybooks 'in no expanded text'. The 'Please look' case he pasted (shapes only): the program lines before the first untied copied line were a COPY whose REPLACING clause runs over several lines - `COPY XXXXXXX9 REPLACING ==XXXX-XXXX.== BY ==XX-999-XXXXX-` and the rest of the pseudo-text on the next line, the period at the very end | `from_ibm_listing` took a COPY statement as closed at the first period on its line - and the period inside the pseudo-text `==XXXX-XXXX.==` counted. The next program line (the rest of the clause) held no COPY, so the tool forgot the statement, and every copied line that followed was untied; the same for a pseudo-text broken across lines | `recover._copy_closed()`: a period inside a literal or inside `==...==` ends nothing, and an odd number of `==` (an open pseudo-text) keeps the statement open on to the next line; the listing generator in tests gathers a multi-line COPY the way the compiler prints it (the whole statement, then the copied lines). Rule for me: '.' in a COBOL statement is closed only outside literals and pseudo-text - three readers had the same test | `test_recover.py` CopyStatementsOverLines (`_copy_closed` line by line; a listing with the period inside the pseudo-text and the BY text on the next line: zero untied lines, the region's statement whole, REPLACING seen) |
| 191 | The same run: 'recovered: 0 of 27 ... 3 rejected' - the three are his procedure copybooks (the `A-100-BEGIN SECTION.  COPY X.` case), whose library copies on disk hold a number and no COBOL. The listings carry their text expanded under the programs, the copybook check read it as procedure code, and the write was refused: 'the build would file this as asm' | recover checks what the build's classifier would make of the copybook it is about to write and refused every kind but copybook / cobol - right for a block that really is something else, wrong for a copybook whose one `START CUSTFILE` line trips the Assembler signature (rows 186, 189): the copybook the programs need was never written, and the report gave no way forward | The write goes through when `refile_verdict()` would re-file the member afterwards (a weak kind by a line of the text, the copybook shape sound, none of the real Assembler / listing / MFS shapes), the report's 'how' column and the console say the build will file it as that kind and that a second recover run after the build re-files it (then build once more), the marker records `misread_as`; a copybook still misfiled or re-filed on a run is not 'recovered on an earlier run, still missing'. Until ROADMAP re-parse item 22 that is two builds; after it, one | `test_refiled_copybooks.py` WrittenThoughTheClassifierMisreadsIt (case K: the listing in the estate, the copybook's library copy a bare number: recover writes it with the note, the build files it asm, recover re-files it and marks the program, the build makes the program whole with the section and the copybook's paragraphs; a real Assembler block is still refused) |
| 192 | 'missing copybooks in the index: 27' on his estate while, as he said, the copybooks sat in the COPYLIB folders under estate\SHARED (`.cpy` files in the folders the UI fetched, `.txt` in the ones he fetched by hand; some with real variables and VALUE clauses over several lines). Only two of the 27 names had a member row at all - both filed 'empty', said with the bare word - and the tool said nothing about the other 25. An evening of questions to him followed ('you really wasted my last few days') | Every scan looked at the INDEX only: missing_copybooks() and arrival_scan() read member rows, so a name with no row got no word, and 'empty' hid what the reader saw - a 7-8 digit number in column 1, which fixed-format COBOL takes for the sequence area and the indicator column, so build.code_line_count gives 0 (VALUE clauses over several lines parse fine: verified, not a cause). The tool must look at the disk and say what it sees; asking the user to search files for it is the failure | `recover.disk_check()`: the estate root walked ONCE (estate_files: what the build lists; 121k files in seconds), then per missing name - a file with that name not in the index (arrived after the last build started: run the build; or there at the last build: the build did not index it - atlas-problems.txt, the coverage 'failed' table), a member filed 'empty' with the columns-1-7 sentence (stub_lines / stub_sentence; refile_verdict and the misfiled cells say it too), the nearest names in the copybook folders (an extension or a suffix in the stem, another spelling), or no file (the fetch list). The report's first section '## Missing copybooks, checked on disk', one console line and one 'next:'; coverage's table gains an 'on disk?' column and `copybook NAME` the same verdict. ROADMAP re-parse item 23 (the reader takes a column-1 stub as code). Rule for me: when the index says 'missing', the next question is the disk, and the tool asks it | `tests/test_disk_check.py` (arrived after the build; near names only; the stub filed empty; nothing near; coverage and `copybook` carry the verdicts; nothing missing changes nothing - the console and the report of aba1a2d to the line; there at the last build; the root moved; 20,000 files under 5 s) |
